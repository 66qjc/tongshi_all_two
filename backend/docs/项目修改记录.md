# 项目修改记录

---

## 第 1 轮：基础安全检查

### 修改时间

2026-05-18

### 修改目标

修复后端代码中的功能正确性问题 — 包括答题计数逻辑 bug、ORM 关系缺失导致的 N+1 查询、文件上传无校验、批量导入事务缺陷四项高优先级问题。

### 修改内容

1. **修复 `quiz_service.submit_answer` 计数逻辑**
   - 文件：`app/services/quiz_service.py`
   - 问题：`session` 配置了 `autoflush=False`，先 `db.add(attempt)` 再执行 `count()` 查询时，新增 attempt 对 count 不可见，导致 `questions_done` 计数不准确
   - 修复：在 count 查询前显式调用 `db.flush()`；`sp.questions_done` 改为直接从 count 结果赋值，而非 `+= 1`

2. **`QuizAttempt` 模型添加 `question` 关系，消除 N+1 查询**
   - 文件：`app/models/entities.py`、`app/services/quiz_service.py`
   - `QuizAttempt` 新增 `question = relationship("Question")`
   - `get_quiz_history` 使用 `joinedload(QuizAttempt.question)` 预加载，替代原先循环内逐条查询 Question 的 N+1 模式

3. **文件上传增加安全校验**
   - 文件：`app/core/upload_validation.py`（新建）、`app/api/v1/routes/upload_routes.py`、`question_routes.py`、`class_routes.py`
   - 通用上传接口：限制允许的文件类型（图片/文档/视频/压缩包）和 50MB 上限
   - Excel 导入接口：限制 `.xlsx` / `.xls` 和 10MB 上限
   - 所有上传接口在 `file.read()` 之后、写磁盘之前做校验，早期拦截非法文件

4. **批量导入事务保护修复**
   - 文件：`app/services/class_service.py`
   - 问题：Excel 导入学生在循环中 catch `SQLAlchemyError` 后调用 `db.rollback()`，导致已成功处理的行也被回滚
   - 修复：每行用 `db.begin_nested()` 保存点包裹，单行失败仅回滚该行，其他行不受影响

### 验证情况

- 所有修改文件通过 Python 编译检查
- `upload_validation` 校验逻辑通过单元测试（正常文件、不支持类型、超限文件、空文件名）

### 遗留问题

无

---

## 第 2 轮：需求文档未实现的功能

### 修改时间

2026-05-19

### 修改目标

对照 `docs/backend-features/` 中的需求文档，实现两个完全未落地的功能模块 — 课程时间安排（06 号需求）和作品批量下载 ZIP（05 号需求）。

### 修改内容

1. **课程时间安排（06-class-schedule.md）**
   - 文件：`app/models/entities.py`、`app/schemas/common.py`、`app/api/v1/routes/chapter_routes.py`、`app/services/chapter_service.py`、`seed_data.py`
   - Chapter 模型新增 `day_of_week`、`class_periods`、`schedule_note` 三个字段
   - 新增 `ChapterScheduleUpdate` Pydantic Schema
   - `ChapterOut` 补充 `course_id`、`day_of_week`、`class_periods`、`schedule_note` 四个字段
   - 新增 `PUT /api/chapters/{id}/schedule` 端点（teacher 权限）
   - `chapter_service.list_chapters()` 和 `chapter_routes.get_chapter_detail()` 全部返回新字段
   - `seed_data.py` 前三章（01/02/03）补充排课时间示例

2. **作品批量下载 ZIP（05-project-upload-enhance.md）**
   - 文件：`app/api/v1/routes/teacher_routes.py`
   - 新增 `GET /api/teacher/projects/batch-download` 端点（teacher 权限）
   - 查询所有 `status='approved'` 且 `report_url` 非空的 Project，从 `uploads/` 读取 PDF 文件
   - 用 `zipfile` 打包为 ZIP（内存中），通过 `StreamingResponse` 返回
   - 文件名：`作品报告_{日期}.zip`，ZIP 内文件：`{作者名}_{标题}.pdf`

### 验证情况

- `Chapter` 模型新字段属性断言通过
- `ChapterScheduleUpdate` Schema 序列化/反序列化通过
- 所有模块成功导入，无语法错误

### 遗留问题

无

---

## 第 3 轮：代码质量与一致性修复

### 修改时间

2026-05-19

### 修改目标

补全 ORM 关系映射、清理仓库无用文件和 SQLite 残留、将路由中裸 `dict` 参数统一改为 Pydantic Schema。

### 修改内容

1. **补全实体模型 relationship**
   - 文件：`app/models/entities.py`、`app/services/announcement_service.py`、`app/services/task_service.py`
   - `Announcement` 新增 `class_`、`teacher`、`reads`、`completions` 四个关系
   - `AnnouncementRead` 新增 `announcement`、`user` 关系
   - `TaskCompletion` 新增 `announcement`、`user` 关系
   - `announcement_service._announcement_payload` 去掉手动 `db.query(Class)` / `db.query(User)` 查询，改用 `ann.class_` / `ann.teacher` / `ann.reads`
   - `task_service.completion_report` 去掉手动 `db.query(Class)`，改用 `ann.class_`

2. **无用文件清理 + .gitignore 更新**
   - 删除 `app/models/announcement_entities_patch.py`（内容仅 "placeholder"）
   - 删除 `tongshi.db-shm`、`tongshi.db-wal` SQLite WAL 残留文件
   - `.gitignore` 新增 `*.db`、`*.db-shm`、`*.db-wal`、`.claude/settings.local.json`

3. **路由 dict 参数 → Pydantic Schema**
   - 文件：`app/schemas/common.py`、`app/api/v1/routes/announcement_routes.py`、`question_routes.py`、`class_routes.py`
   - 新增 `AnnouncementCreate`、`CourseCreateRequest`、`CourseUpdateRequest`、`ClassEnrollRequest` 四个 Schema
   - `announcement_routes.create(data: dict)` → `create(data: AnnouncementCreate)`
   - `question_routes.add_course(payload: dict)` / `edit_course(payload: dict)` → 改用 `CourseCreateRequest` / `CourseUpdateRequest`
   - `class_routes.add_student(student_id: str)` query 参数 → `ClassEnrollRequest` body

### 验证情况

- 所有新 Schema 构造和序列化通过
- 实体关系属性存在性断言通过
- 删除的文件确认不再存在于磁盘和 git 跟踪

### 遗留问题

无

---

## 第 4 轮：种子数据完善 + 密码安全 + 框架升级

### 修改时间

2026-05-19

### 修改目标

完善开发用种子数据（班级/课程/公告）、增强注册密码复杂度校验、修复 FastAPI `on_event` 弃用警告。

### 修改内容

1. **种子数据完善**
   - 文件：`seed_data.py`
   - 新增 `Course`：创建 "AI 通识课"，6 个章节全部关联 `course_id`
   - 新增 `Class` + `StudentClassEnrollment`：创建 "2025级1班"（自动化）和 "2025级2班"（机械工程），5 名学生分配到两个班级
   - 新增 `Announcement`：2 条示例（课程通知 + 章节练习任务，含起止时间）

2. **密码复杂度增强**
   - 文件：`app/schemas/common.py`
   - `RegisterRequest.password` 新增 `@field_validator`：密码必须包含至少一个字母 + 至少一个数字，保留原有 `min_length=6`

3. **FastAPI 框架升级**
   - 文件：`main.py`
   - `@app.on_event("startup")` → `asynccontextmanager lifespan` 模式，消除启动时的 DeprecationWarning

### 验证情况

- 密码校验器：纯数字 "123456" 拒绝、纯字母 "abcdef" 拒绝、混合 "abc123" 通过
- `seed_data` 模块成功导入
- `main.py` 语法正确

### 遗留问题

无

---

## 第 5 轮：测试体系 + API 文档 + Alembic 迁移 + 操作日志

### 修改时间

2026-05-19

### 修改目标

建立集成测试体系、为所有端点添加 Swagger 文档注释、引入 Alembic 数据库迁移机制、在关键服务操作中添加操作日志。

### 修改内容

1. **API 集成测试**
   - 文件：`tests/conftest.py`（新建）、`tests/test_auth.py`（新建）
   - 使用 `pytest` + `httpx`，SQLite 内存数据库（`sqlite:///:memory:`），每次测试独立建库
   - 14 个测试用例覆盖：登录成功/密码错误/不存在用户、密码校验拒绝、重复注册、获取当前用户、章节列表/详情/不存在/权限拒绝、答题提交（正确+错误）/统计/历史
   - `conftest.py` 提供 `client`、`student_token`、`teacher_token` 等 fixture

2. **Swagger 文档注释**
   - 文件：全部 10 个路由文件
   - 每个端点添加 `summary`（中文短描述）和 `description`（中文详细说明），覆盖认证、章节、资料、题库、测验、作品、教师、班级、公告、成长档案、上传所有模块

3. **Alembic 数据库迁移**
   - 文件：`alembic.ini`、`migrations/env.py`、`migrations/script.py.mako`（新建）；`requirements.txt`
   - 安装并初始化 Alembic，`migrations/env.py` 自动读取 `app.core.config.settings.database_url` 作为迁移目标数据库
   - 后续修改模型后执行 `alembic revision --autogenerate -m "说明"` + `alembic upgrade head`

4. **服务层操作日志**
   - 文件：`app/services/auth_service.py`、`announcement_service.py`、`class_service.py`、`project_service.py`
   - 登录成功/失败 → `logger.info/warning` 记录 user_id 和 role
   - 注册成功 → 记录 user_id 和 role
   - 公告/班级创建和删除 → 记录操作者 ID 和对象信息
   - 作品提交/审核通过/驳回 → 记录 user_id、title、reason

### 验证情况

- `python -m pytest tests/ -v` → **14/14 passed**（耗时 1.3s）
- 所有模块通过编译检查

### 遗留问题

- 测试覆盖仅限核心模块（认证 + 章节 + 答题），公告、班级、作品等模块的测试可以后续补充
- Alembic 初始迁移需在 MySQL 可用后执行 `alembic revision --autogenerate -m "init"`

---

## 第 6 轮：课程体系管理联动

### 修改时间

2026-05-21

### 修改目标

将"课程"从独立名称管理完善为课程、章节、资料、课表和学生端学习入口的一级组织体系。

### 修改内容

1. **后端课程与章节接口完善**
   - 文件：`app/api/v1/routes/chapter_routes.py`、`app/api/v1/routes/question_routes.py`、`app/services/chapter_service.py`、`app/services/question_service.py`、`app/schemas/common.py`
   - 章节列表新增 `course_name` 返回字段
   - 新增教师端章节创建、完整编辑、删除接口
   - 章节排课接口支持同步修改 `course_id`
   - 课程列表返回 `chapter_count` 和 `material_count`
   - 新增课程详情接口，返回课程基本信息、统计和该课程下的章节列表
   - 删除课程时，如果课程下仍有章节，返回中文业务错误，避免误删课程结构
   - 删除章节时，如果章节下仍有资料、题目或学习记录，返回中文业务错误，避免误删教学内容

2. **教师端资料管理页联动课程和章节**
   - 文件：`frontend/src/views/teacher/TeacherMaterials.vue`
   - "管理课程"弹窗显示课程名称、章节数、资料数和创建时间
   - 课程详情抽屉展示课程下的章节
   - 支持新增、编辑、删除章节
   - "课程时间安排"按课程分组展示章节
   - 设置课表时可修改章节所属课程、星期、节次和备注

3. **学生端课程列表和课程详情页**
   - 文件：`frontend/src/views/LearnView.vue`、`frontend/src/views/CourseDetailView.vue`、`frontend/src/router/index.ts`
   - `/learn` 改为课程列表，显示每门课程的章节数和资料数
   - 新增 `/learn/course/:courseId` 课程详情页，展示该课程下章节、资料数量、排课和学习入口
   - 原章节学习页 `/learn/:chapterId` 保留，继续承载视频和 PDF 学习

4. **旧数据库字段兼容**
   - 文件：`app/db/schema_compat.py`、`main.py`、`database_setup.py`
   - 后端启动和数据库初始化时自动补齐旧 `chapters` 表缺失的排课字段：`day_of_week`、`class_periods`、`schedule_note`

### 验证情况

- `py -m pytest backend\tests -q` → **23 passed**，仅保留既有 Pydantic V2 `Config` 弃用警告
- `cd frontend && npm run build` → **通过**，仅保留 Vite chunk 体积提示

### 遗留问题

- 尚未用浏览器手动验证 `/teacher/materials`、`/learn`、`/learn/course/:courseId` 的实际交互路径
- 部分旧 Markdown 文档在终端中显示为乱码，未在本轮大规模重写

---

## 第 7 轮：教师数据隔离权限设计

### 修改时间

2026-05-22

### 修改目标

设计教师端数据隔离方案，确保不同教师之间的班级、课程、章节、资料、题目互不串扰。本轮产出设计文档和实施计划，尚未全面落地。

### 修改内容

1. **设计文档和实施计划**
   - 新建：`docs/superpowers/specs/2026-05-22-teacher-data-isolation-design.md`
   - 新建：`docs/superpowers/plans/2026-05-22-teacher-data-isolation-plan.md`
   - 设计文档覆盖：数据模型（5 张表加 `teacher_id`）、接口隔离、旧数据兼容策略
   - 实施计划分 5 个 Task（TDD 步骤），涉及 18 个文件

2. **前端错误提示优化**
   - 文件：`frontend/src/api/http.ts`
   - 新增 `getErrorMessage` 和 `normalizeValidationMessage` 函数
   - 后端 Pydantic 校验错误（如密码长度、必填字段）自动转为中文提示
   - 422 错误统一显示"提交内容不符合要求，请检查填写内容"

3. **注册页面微调**
   - 文件：`frontend/src/views/RegisterView.vue`
   - 适配新的错误提示机制

### 验证情况

- 设计文档和实施计划已提交，待后续按 Task 逐步实施

### 遗留问题

- 教师数据隔离的后端实施（Task 1-5）尚未启动，需要按计划 TDD 步骤逐步推进
- 后续实施完成后需回填本轮修改记录

---

## 第 8 轮：作品审核的改进

### 修改时间

2026-05-23

### 修改目标

完善作品管理功能：支持多图片上传、改进审核流程、优化作品展示和提交体验。

### 修改内容

1. **新增作品图片模型**
   - 文件：`app/models/entities.py`、`app/schemas/common.py`
   - 新增 `ProjectImage` 模型，表 `project_images`（id、project_id、image_url、sort_order）
   - `Project` 新增 `images` 关系，级联删除
   - 新增 `ProjectImageOut` Schema，`ProjectOut` 增加 `images` 字段
   - 新增 `ProjectUpdate` Schema，支持 `image_urls` 列表

2. **作品服务功能增强**
   - 文件：`app/services/project_service.py`
   - 支持多图片创建和更新（`image_urls` 列表）
   - 改进审核逻辑和作品数据聚合

3. **数据库兼容扩展**
   - 文件：`app/db/schema_compat.py`、`tests/test_schema_compat.py`
   - 自动创建 `project_images` 表（旧库兼容）
   - 新增兼容性测试

4. **前端作品体验优化**
   - 文件：`frontend/src/views/ActView.vue`、`ProjectDetailView.vue`、`ProjectUploadView.vue`
   - 活动页面大幅重构（612 行变动）
   - 作品详情页支持图片展示
   - 作品上传页支持多图片

5. **教师端改进**
   - 文件：`frontend/src/views/teacher/TeacherReviews.vue`、`TeacherQuestions.vue`
   - 作品审核页面增加详情展示
   - 题库管理页面交互优化

### 验证情况

- `py -m pytest backend\tests -q` → 通过
- `cd frontend && npm run build` → 通过

### 遗留问题

- 尚未用浏览器手动验证多图片上传和作品详情展示的实际交互路径

---

## 第 9 轮：多存储改造（数据库重建）

### 修改时间

2026-05-25

### 修改目标

引入 SeaweedFS (S3) 作为新文件默认存储，建立统一文件元数据（StoredFile）与存储抽象层，并兼容历史 `backend/uploads` 文件与现有前端接口。依据 `docs/superpowers/plans/数据库重建.md` 的 Task 1~7 全部实施。

### 修改内容

1. **存储配置与依赖**
   - 文件：`app/core/config.py`、`requirements.txt`、`backend/README.md`
   - 新增 9 个存储配置字段（`storage_backend`、`s3_endpoint`、`s3_access_key` 等）
   - 新增依赖：`boto3`、`python-magic`

2. **文件元数据模型 StoredFile**
   - 文件：`app/models/entities.py`、`app/schemas/common.py`、`app/db/schema_compat.py`
   - 新增 `StoredFile` 模型（15 列：id、biz_type、storage_provider、object_key、content_type、sha256 等）
   - `Material` 新增 `file_id`，`Project` 新增 `report_file_id`/`cover_file_id`，`ProjectImage` 新增 `file_id`
   - Schema 兼容：`UploadOut` 增加 `file_id`/`content_type`/`storage_provider`，业务 Schema 增加 `file_id`
   - `schema_compat.py` 自动创建 `stored_files` 表 + 补齐新列

3. **存储抽象层**
   - 新建：`app/services/storage_service.py`（StoredObject dataclass + StorageAdapter Protocol）
   - 新建：`app/services/storage_local.py`（LocalStorageAdapter，读写本地 uploads 目录）
   - 新建：`app/services/storage_s3.py`（S3StorageAdapter，boto3 实现）
   - 新建：`app/services/file_service.py`（文件元数据写入、URL 构建、记录解析）

4. **统一文件访问路由**
   - 新建：`app/api/v1/routes/file_routes.py`（`GET /files/{file_id}` 返回 FileResponse/StreamingResponse）
   - 在 `app/api/v1/__init__.py` 注册 file_router
   - `main.py` 保留 `/uploads` 静态挂载，兼容历史 URL

5. **上传安全升级**
   - 文件：`app/core/upload_validation.py`、`app/api/v1/routes/upload_routes.py`
   - 新增 `detect_content_type()` 魔数检测（PDF/ZIP/PNG/JPEG/GIF/WebP）
   - 上传路由改造：读 content → 魔数检测 → 存储适配器保存 → 创建 StoredFile → 返回 file_id

6. **资料/作品/批量下载接线**
   - 文件：`app/services/material_service.py`、`material_routes.py`、`project_service.py`、`project_routes.py`、`teacher_routes.py`
   - 资料创建支持 `file_id`，路由返回 `file_id`
   - 作品创建/更新支持 `report_file_id`/`cover_file_id`/`image_file_ids`
   - 教师批量下载改为临时 ZIP 文件 + FileResponse（避免内存常驻）

7. **前端兼容**
   - 文件：`frontend/src/api/upload.ts`、`material.ts`、`project.ts`
   - `UploadResult` 增加 `file_id`/`content_type`/`storage_provider`
   - `Material`/`Project` 类型增加 `file_id` 相关字段

8. **测试覆盖**
   - 文件：`tests/test_schema_compat.py`（+3 测试）、`tests/test_integration_bugfixes.py`（+7 测试）
   - 新增 10 个测试：配置默认值、StoredFile 表创建、文件路由、上传返回 file_id、魔数校验、资料/作品 file_id 持久化、批量下载

9. **文档与忽略规则**
   - `.gitignore` 新增 `*.sqlite`、`*.sqlite3`、`backend/tmp/`、`backend/exports/`、`*.zip.tmp`
   - `backend/README.md` 更新目录结构、存储配置说明
   - `CLAUDE.md` 更新表数（16→17）、路由数（11→12）、存储说明

### 验证情况

- `py -m pytest backend\tests -q` → **39 passed**（含 10 个新增存储测试）
- `cd frontend && npm run build` → 通过
- Windows 环境修复：`fcntl` 模块条件导入、SQLAlchemy inspector 缓存刷新、PDF 魔数偏移修复

### 遗留问题

- 未验证真实 SeaweedFS S3 连接（本地使用 local 存储后端）
- 未验证 S3 权限错误分支、千兆级大包下载性能

---

## 第 10 轮：P1 — 批量下载 S3 适配改造

### 修改时间

2026-05-26

### 修改目标

修复批量下载在 S3 存储模式下完全不可用的问题。原实现硬编码了本地 `uploads` 路径且仅支持 `report_url`（旧字段），改为通过 `file_service.resolve_file_stream()` 统一获取文件流，自动适配 local/s3 两种存储后端，同时实现流式 ZIP 输出控制内存峰值。

依据：`docs/superpowers/specs/2026-05-26-c-group-next-steps-design.md` 大计划 P1。

### 修改内容

1. **文件定位逻辑统一（P1.1）**
   - 文件：`app/api/v1/routes/teacher_routes.py`
   - 移除硬编码路径 `Path(__file__).resolve().parents[4] / "uploads"`
   - 新增 `_get_project_file_content()` 函数，通过 `resolve_file_stream()` 统一获取文件流
   - S3 文件和历史本地文件使用同一入口，自动分发

2. **新旧字段兼容（P1.2）**
   - 优先通过 `report_file_id` → `stored_files` 表 → 存储适配器获取文件
   - 降级到 `report_url` → `settings.local_upload_dir` 读取历史本地文件
   - 查询条件从仅 `report_url` 扩展为两者 OR 条件

3. **流式 ZIP 打包（P1.3）**
   - ZIP 写入临时磁盘文件（避免所有文件同时驻留内存）
   - 通过 `_chunk_reader()` 生成器 64KB 分块 yield 给 `StreamingResponse`
   - 使用 `BackgroundTasks` 在响应发送完成后自动清理临时文件

4. **文件名安全增强（P1.4）**
   - 新增 `_sanitize_filename()` 函数，过滤控制字符（ASCII <32、DEL 127）、路径分隔符和通配符
   - 空文件名降级为 "unnamed"

### 验证情况

- `python -m pytest tests/ -v` → **39/39 passed**，无回归
- 两个原有批量下载测试（`test_batch_download_uses_standard_auth_header`、`test_batch_download_returns_zip_for_approved_projects`）均通过
- 历史作品（仅 `report_url`）和新作品（含 `report_file_id`）均可正常下载

### 遗留问题

- 本次仅修复了批量下载的文件定位和流式输出问题；魔数校验漏洞在后续 P3 中处理

---

## 第 11 轮：P2 — S3 存储适配器修复

### 修改时间

2026-05-27

### 修改目标

加固 S3 存储适配器的健壮性：添加 boto3 重试策略应对临时网络故障、`open_stream` 捕获 404 错误避免未处理异常导致 500、`resolve_file_stream` 加强异常兜底。

依据：`docs/superpowers/specs/2026-05-26-c-group-next-steps-design.md` 大计划 P2。

注：P2.1（`s3_force_path_style` 传递）已在上游 PR #2 合并时修复，本轮仅补做 P2.2 和 P2.3。

### 修改内容

1. **添加 boto3 重试策略（P2.2）**
   - 文件：`app/services/storage_s3.py`
   - `S3StorageAdapter.__init__` 始终配置 `retries={"max_attempts": 3, "mode": "standard"}`
   - 重构 `Config` 构建逻辑：使用 `config_kwargs` 字典统一管理 `retries` 和条件 `s3.addressing_style`

2. **open_stream 404 错误处理（P2.3）**
   - 文件：`app/services/storage_s3.py`
   - `open_stream` 捕获 `botocore.exceptions.ClientError`，识别 `NoSuchKey`/`404` 错误码
   - 转换为 Python 标准 `FileNotFoundError`（不带 Web 框架依赖），携带 bucket/key 上下文
   - 其他 `ClientError` 继续向上抛出

3. **resolve_file_stream 异常兜底**
   - 文件：`app/services/file_service.py`
   - `resolve_file_stream` 对 `open_stream` 调用增加 try/except
   - 捕获 `FileNotFoundError` 和其他异常，统一返回 `(record, None)`
   - 防止 `exists()` 和 `open_stream()` 之间的竞态条件导致 500

### 验证情况

- `python -m pytest tests/ -v` → **40/41 passed**（1 个预存失败：`test_settings_reads_seaweedfs_s3_config` 因 `load_dotenv(override=True)` 覆盖 monkeypatch 导致，与本次改动无关）
- C 组相关全部测试（上传、文件访问、批量下载、存储 schema 兼容）均通过

### 遗留问题

- S3 适配器仍无基于 moto mock 的单元测试（计划在 P4 中补齐）
- `save_bytes` 和 `delete` 未添加重试/异常处理（当前影响较小，可后续统一加固）

---

## 第 11 轮：超级大更改（管理员系统 + 个人中心 + 悟页面 + 课程管理 + 登录安全增强）

### 修改时间

2026-05-27

### 修改目标

一次性落地杨力提出的功能需求：管理员教师账号管理系统、学生个人中心（错题本+收藏）、悟页面图文内容管理、教师独立课程管理页面，以及首次登录强制改密等安全增强。共涉及 45 个文件，4393 行新增。

### 修改内容

1. **管理员系统（admin 角色）**
   - 后端：新建 `admin_routes.py`（`/api/admin/teachers`），支持教师账号 CRUD、Excel 批量导入、密码重置
   - `security.py`：`get_current_user` 返回增加 `needs_password_change` 字段；支持 `require_role("admin")`
   - `entities.py`：`User` 模型新增 `needs_password_change = Column(Boolean, default=False)`
   - `schemas/common.py`：新增 `CreateTeacherRequest`、`TeacherInfo`、`ChangePasswordRequest`、`ForgotPasswordRequest`
   - `auth_service.py`：新增 `forgot_password()` 函数
   - 前端：新建 `admin.ts`（API 封装）、`AdminLayout.vue`、`AdminTeachers.vue`、`AdminShowcase.vue`
   - 种子数据：`seed_data.py` 新增管理员账号

2. **学生个人中心**
   - 后端：新建 `profile_routes.py`（`/api/profile`），提供错题本 + 收藏作品两个接口
   - `quiz_service.py`：新增 `get_wrong_questions()`，取每道题最近一次答错记录
   - `project_service.py`：新增 `list_liked_projects()`，返回用户点赞过的所有作品
   - 前端：新建 `profile.ts`（API 封装）、`ProfileView.vue`（错题本 + 收藏作品 Tab 页）、`ChangePasswordView.vue`

3. **悟页面图文内容管理（Showcase）**
   - `entities.py`：新增 `ShowcaseItem` 模型，表 `showcase_items`（id、section、title、content、cover_file_id、link_url、sort_order、is_active、created_by、created_at、updated_at）
   - `schemas/common.py`：新增 `ShowcaseItemCreate`、`ShowcaseItemUpdate`、`ShowcaseItemOut`
   - 后端：新建 `showcase_routes.py`（`/api/showcase`），管理员增删改查 + 公开只读，按 section（welfare/reading_club）分组
   - 前端：新建 `showcase.ts`（API 封装）、`AdminShowcase.vue`

4. **教师课程管理页面**
   - 前端：新建 `TeacherCourses.vue`，独立的课程管理视图
   - `TeacherLayout.vue`：导航菜单新增"课程管理"入口

5. **登录安全增强**
   - `needs_password_change` 字段：新建教师账号时标记为 `True`，首次登录必须改密
   - `router/index.ts`：新增 `beforeEach` 守卫，已登录且 `needs_password_change=true` 时强制跳转 `/change-password`
   - 管理员访问首页自动跳转 `/admin` 后台
   - 管理员路由新增 `role: 'admin'` 鉴权守卫
   - `auth.ts`（前端）：`changePassword()` 和 `forgotPassword()` API

6. **其他前端增强**
   - 平台名称从"探 · 练 · 创 · 行"改为"学 · 思 · 践 · 悟"
   - `ActView.vue`：大幅重构（394+ 行变更）
   - `TeacherAnnouncements.vue`：公告管理功能增强（387+ 行变更）
   - `TeacherQuestions.vue`：题库管理增强（263+ 行变更），`QuestionOut` Schema 增加 `chapter_name`、`course_id`、`course_name`
   - `LoginView.vue`：登录页增强（177+ 行变更），支持忘记密码
   - `ClassEnrollRequest`：新增 `name` 字段，注册时可同时指定姓名
   - 首页组件调整：`AppHeader.vue`、`AppFooter.vue`、`HeroSection.vue`、`ModuleShowcase.vue` 等

7. **数据库兼容**
   - `schema_compat.py`：新增自动补齐 `users.needs_password_change` 列
   - `schema_compat.py`：新增自动创建 `showcase_items` 表（需在 `ensure_schema_compatibility` 中添加）

8. **路由注册**
   - `__init__.py`：新增 `admin_router`（prefix="/admin"）、`profile_router`、`showcase_router` 三个路由注册

### 验证情况

- 前后端代码语法正确
- **缺少实际测试**：本次变更涉及 45 个文件但未补充对应的集成测试

### 遗留问题

- 管理员 CRUD、个人中心错题本/收藏、悟页面 CRUD、密码修改/重置流程均无测试覆盖
- `showcase_items` 表在 `schema_compat.py` 中的自动创建逻辑可能需要补充（当前 diff 中未见到）
- `QuestionOut` Schema 新增字段需要确认前端 TeacherQuestions.vue 的展示逻辑是否完整
- 部分旧 Markdown 文档（如 CLAUDE.md 中的路由/表计数）需同步更新

---

## 第 12 轮：S3 文件预览与 MySQL 运行配置修复

### 修改时间

2026-05-28

### 修改目标

保留 SeaweedFS/S3 中已有文件，不迁回本地；业务数据继续使用 MySQL。修复学生端章节资料中视频和 PDF 无法在浏览器正常查看的问题。

### 修改内容

1. **统一文件访问支持浏览器 Range 请求**
   - 文件：`app/api/v1/routes/file_routes.py`
   - `GET /api/files/{file_id}` 新增 `Range` 请求解析
   - 对合法范围返回 `206 Partial Content`
   - 响应头包含 `Accept-Ranges`、`Content-Range`、`Content-Length`
   - 解决浏览器 `<video>` 播放和 PDF 预览需要分段读取的问题

2. **保持 MySQL + S3 运行配置**
   - 文件：`backend/.env`
   - `DATABASE_URL` 使用 `mysql+pymysql://.../tongshi`
   - `STORAGE_BACKEND=s3`
   - SeaweedFS S3 网关继续使用 `S3_ENDPOINT=http://localhost:8333`

3. **教师端资料上传保存 file_id**
   - 文件：`frontend/src/views/teacher/TeacherMaterials.vue`
   - 创建资料时提交上传接口返回的 `file_id`
   - `materials.file_id` 可稳定指向 `stored_files.id`
   - 前端仍通过 `/api/files/{file_id}` 访问文件，不直接暴露 SeaweedFS 对象地址

4. **回归测试补充**
   - 文件：`backend/tests/test_integration_bugfixes.py`
   - 新增 `/api/files/{file_id}` Range 分段访问测试

### 验证情况

- `cmd /c backend\start_backend.bat -CheckOnly` 通过：MySQL 可连接，SeaweedFS S3 网关可达，后端健康检查可用
- `curl` 请求现有视频 `/api/files/2` 返回 `206 Partial Content`，`content-type: video/mp4`
- `curl` 请求现有 PDF `/api/files/1` 返回 `%PDF-1.5`，`content-type: application/pdf`
- `python -m pytest backend/tests/test_integration_bugfixes.py::TestIntegrationBugfixes::test_get_file_by_file_id_returns_streamed_content backend/tests/test_integration_bugfixes.py::TestIntegrationBugfixes::test_get_file_by_file_id_supports_range_requests backend/tests/test_integration_bugfixes.py::TestIntegrationBugfixes::test_create_material_persists_file_id -q` 通过（3 passed）
- `cmd /c npm run build` 通过

### 遗留问题

- 当前验证使用真实本地 SeaweedFS 网关和已有文件记录，尚未补充 S3 mock 单元测试
- PowerShell 直接执行 `npm run build` 会被本机执行策略拦截，Windows 下可用 `cmd /c npm run build`
- 当前 shell 中 `git` 不可用，无法通过 `git status` 核对工作区状态

---

## 第 13 轮：上传安全加固 + 前端体验优化（P3 + P5）

### 修改时间

2026-05-30

### 修改目标

堵住文件上传魔数校验漏洞（MP4/MOV/WebP）、补齐 DOCX/PPTX 容器格式检查和 SVG 安全清洗；同时优化前端上传和审核体验（biz_type 分类、图片缩略图、搜索筛选、文案统一）。

依据：`docs/superpowers/specs/2026-05-26-c-group-next-steps-design.md` 大计划 P3 + P5。

### 修改内容

**P3 — 上传安全校验加固**

1. **MP4/MOV 魔数修复**
   - 文件：`app/core/upload_validation.py`
   - 问题：旧校验 `[b"\x00\x00\x00", b"ftyp"]` 对任意以三个 null 字节开头的文件放行，ftyp 未限定偏移
   - 修复：`validate_magic_number` 对 `.mp4`/`.mov` 校验偏移 4 字节处必须为 `ftyp`（ISOBMFF 标准）

2. **WebP 双重校验**
   - 文件：`app/core/upload_validation.py`
   - 修复：校验偏移 0 处为 `RIFF` **且** 偏移 8 处为 `WEBP`，防止 AVI/WAV 误过

3. **DOCX/PPTX ZIP 容器 PK 头检查**
   - 文件：`app/core/upload_validation.py`
   - 修复：将 `.docx`/`.pptx` 从 `_SKIP_MAGIC_EXTENSIONS` 移除，添加 `PK` 头校验
   - `detect_content_type` 新增 docx/pptx MIME 类型推断
   - `.svg`（XML 文本格式）和 `.doc`（旧格式魔数复杂）保留跳过

4. **SVG 安全清洗器**
   - 新建：`app/core/svg_sanitizer.py`
   - 移除危险标签（`<script>`、`<foreignObject>`、`<use>`）
   - 移除 `on*` 事件属性（`onclick`、`onload` 等）
   - 移除 `<!DOCTYPE` 声明和 `<!ENTITY` 外部实体引用
   - 集成到 `upload_routes.py`：SVG 上传时先清洗再存储，清洗后为空则拒绝

**P5 — 前端体验优化**

5. **上传增加 biz_type 分类**
   - 文件：`upload_routes.py` + `upload.ts` + `ProjectUploadView.vue`
   - 后端接收 `biz_type` 表单字段（默认 `"upload"`），写入 `stored_files.biz_type`
   - 前端：报告上传传 `"project_report"`，图片上传传 `"project_image"`
   - 同时补齐 `report_file_id` 和 `image_file_ids` 的提交

6. **编辑作品时显示图片缩略图**
   - 文件：`ProjectUploadView.vue`
   - 已有图片 URL 渲染为 32×32 缩略图（`<img>` 标签），可直观辨认已上传图片

7. **审核列表搜索 + rejected 筛选**
   - 后端：`teacher_service.py` `list_all_projects` 新增 `keyword` 参数（`title LIKE`）
   - 后端：`teacher_routes.py` `GET /teacher/projects` 新增 `keyword` 查询参数
   - 后端：去掉默认 `status.in_(["pending", "approved"])` 硬过滤，使 `rejected` 筛选生效
   - 前端：`TeacherReviews.vue` 新增搜索框（支持回车搜索 + 清除重置）
   - 前端：状态筛选下拉补全 `rejected`（驳回）选项
   - 前端：`teacher.ts` `getAllProjects` 新增 `keyword` 参数

8. **文件大小限制统一 50MB**
   - 文件：`upload_validation.py` + `ProjectUploadView.vue`
   - 后端 `MAX_UPLOAD_SIZE` 从 2GB 改为 50MB
   - 前端上传提示文案从"限 20MB"改为"限 50MB"

### 验证情况

- `python -m pytest tests/ -v` → **59/60 passed**（1 个预存失败：`test_settings_reads_seaweedfs_s3_config` monkeypatch 问题，与本次改动无关）
- 魔数校验：MP4/MOV 只认偏移 4 的 `ftyp`；WebP 双重校验 RIFF+WEBP；DOCX/PPTX 检查 PK 头
- SVG 清洗：含 `<script>` 的 SVG 被清洗干净，空结果被拒绝
- 前端搜索框 + rejected 筛选可以正常过滤作品列表

### 遗留问题

- 无需特殊处理

---

## 第 14 轮：测试体系补齐 + 代码质量提升（P4 + P6）

### 修改时间

2026-05-30

### 修改目标

补齐 S3 适配器、作品审核、点赞、完整流程、批量下载五大关键路径的自动化测试；消除 `_format_project` 重复代码、计算 SHA-256 文件指纹、为 `ProjectLike` 添加数据库唯一约束、点赞改为原子化 UPDATE。

依据：`docs/superpowers/specs/2026-05-26-c-group-next-steps-design.md` 大计划 P4 + P6。

### 修改内容

**P4 — 测试体系补齐**

1. **S3 适配器单元测试（moto mock）**
   - 新建：`tests/test_storage_s3.py`（7 个测试）
   - 覆盖：`save_bytes` → `open_stream` 读写闭环、私有桶、`exists`、`delete`、`open_stream` 404 → `FileNotFoundError`、`force_path_style` 配置传递
   - 使用 `moto[s3]` mock，不依赖真实 S3 服务

2. **审核接口集成测试**
   - 文件：`tests/test_integration_bugfixes.py`（4 个测试）
   - 覆盖：教师通过/驳回审核（含驳回理由）、非教师审核拒绝（403）、审核不存在作品返回 404

3. **点赞接口集成测试**
   - 文件：`tests/test_integration_bugfixes.py`（4 个测试）
   - 覆盖：点赞后 liked=true + likes=1、取消点赞 liked=false + likes=0、重复点赞不累积、点赞不存在作品返回 404

4. **作品完整流程 E2E 测试**
   - 文件：`tests/test_integration_bugfixes.py`（2 个测试）
   - 覆盖：创建 → pending 不在广场 → 审核通过 → 广场可见；驳回 → 重新提交 → 状态变回 pending

5. **批量下载测试**
   - 文件：`tests/test_integration_bugfixes.py`（3 个测试）
   - 覆盖：无可下载作品返回 404、本地存储下载返回 ZIP、通过 `report_file_id`（新方式）下载

**P6 — 代码质量与工程化**

6. **统一 `format_project`，消除重复代码**
   - 文件：`project_service.py` + `project_routes.py` + `teacher_routes.py`
   - `project_service.format_project()` 作为唯一规范版本，包含 `report_file_id`/`cover_file_id`
   - 删除 `project_routes.py` 和 `teacher_routes.py` 各自重复的 `_format_project` 函数
   - 清理无用 import（`User` 模型不再在路由层直接查询）

7. **上传时计算 SHA-256**
   - 文件：`upload_routes.py`
   - 上传成功后对内容做 `hashlib.sha256(content).hexdigest()`
   - 传入 `create_stored_file_record(sha256=...)` 存入 `stored_files.sha256`

8. **ProjectLike 添加唯一约束**
   - 文件：`entities.py`
   - `ProjectLike.__table_args__` 新增 `UniqueConstraint("user_id", "project_id", name="uq_project_like")`
   - 数据库层面兜底防止重复点赞

9. **点赞改为原子 UPDATE**
   - 文件：`project_service.py` `toggle_like`
   - 点赞数递增/递减改为 `UPDATE projects SET likes = likes +/- 1 WHERE id = :id`
   - 避免 SELECT + UPDATE 竞态条件导致点赞数不准确

10. **教师课程隔离**（上游 merge 已实现）
   - `teacher_service.py` 的 `list_all_projects` 通过 `_teacher_student_ids` 过滤教师课程下班级学生的作品
   - 教师只能审核/下载自己学生的作品

### 验证情况

- `python -m pytest tests/ -v` → **59/60 passed**（预存 1 个失败同上）
- S3 moto mock 7 个测试全部通过
- 审核、点赞、流程、批量下载 13 个新增集成测试全部通过
- `format_project` 统一后，作品广场、我的作品、作品详情、审核列表等路由行为一致
- `toggle_like` 原子 UPDATE 后并发安全性提升

### 遗留问题

- 无

---

## 第 15 轮：学生登录空白页修复 + 忘记密码安全改造（密保验证）

### 修改时间

2026-06-03

### 修改目标

1. 修复学生登录后首页内容空白、"需要老师权限"错误弹窗的线上 bug
2. 彻底改造忘记密码流程：从「只需学号即可重置」升级为「密保问题验证 + 人工审批」双通道方案，堵住身份冒用安全漏洞

### 修改内容

**一、学生登录 bug 修复**

1. **公告未读数接口路由注册错误修复**
   - 文件：`backend/app/api/v1/routes/announcement_routes.py`
   - 问题：`@router.get("/unread-count")` 装饰器缺少函数体，Python 将其与下方的 `/task-overview` 装饰器双叠包裹了 `overview()`（teacher 权限），导致学生调用时返回 403 "需要teacher权限"
   - 修复：将学生端函数 `get_unread_count` 移至 `/unread-count` 装饰器正下方

2. **学生登录首页空白修复**
   - 文件：`frontend/src/App.vue`
   - 问题：`<Transition mode="out-in">` 与 `router.replace('/')` 组合使用时，旧组件离开后新组件进入前出现空白间隙。只有学生端（跳转 `/`）受影响，教师/管理员端（跳转 `/teacher`/`/admin`）因跳过 Transition 不受影响
   - 修复：移除 `mode="out-in"`，改为默认模式（新旧同时过渡）

**二、忘记密码安全改造**

3. **数据库新增两张表**
   - 文件：`backend/app/models/entities.py`、`backend/app/db/schema_compat.py`
   - `security_questions`（id、user_id、question、answer_hash、created_at）：存储用户密保问题，答案使用 pbkdf2_sha256 哈希
   - `password_reset_requests`（id、user_id、message、status、resolved_by、new_password_hash、temp_password、resolved_at、created_at）：存储人工密码重置申请和审批记录

4. **新增 Pydantic Schema**
   - 文件：`backend/app/schemas/common.py`
   - 新增 8 个 Schema：`SecurityQuestionItem`、`SecurityQuestionIn`、`SecurityQuestionsUpdate`、`SecurityQuestionOut`、`ForgotPasswordCheckRequest`、`ForgotPasswordManualRequest`、`ResetRequestResolve`、`ResetRequestOut`
   - 旧 `ForgotPasswordRequest` 保留兼容

5. **密保问题管理服务**
   - 文件：`backend/app/services/auth_service.py`
   - `get_security_questions()` / `update_security_questions()`：CRUD，最多 3 个密保问题，整体替换
   - `get_forgot_password_questions()`：忘密页第一步，只返回问题文本
   - `verify_answers_and_reset_password()`：验证全部答案后重置密码
   - `submit_reset_request()`：提交人工重置申请（含留言，已有 pending 时拒绝重复）
   - `get_reset_requests_for_teacher()` / `get_reset_requests_for_admin()`：按角色和 status 筛选请求列表
   - `approve_reset_request()`：生成 8 位随机临时密码 → 重置 → `needs_password_change=true` → 存储 temp_password
   - `reject_reset_request()`：驳回申请

6. **密保验证暴力破解防护**
   - 文件：`backend/app/services/auth_service.py`
   - 新增 `_FORGOT_FAILURES` 内存计数器：同一 user_id 5 分钟内最多 5 次错误尝试
   - 超限返回 429 → 前端弹窗引导一键转人工申请
   - 验证通过后清除失败计数

7. **新增 API 端点**
   - 文件：`backend/app/api/v1/routes/auth_routes.py`
   - `GET /api/security-questions`（获取密保问题）
   - `PUT /api/security-questions`（设置密保问题）
   - `GET /api/password/forgot/questions?user_id=xxx`（忘密第一步）
   - `POST /api/password/forgot/reset`（答对后重置）
   - `POST /api/password/forgot/request`（提交人工申请）
   - 文件：`backend/app/api/v1/routes/teacher_routes.py`
   - `GET /api/teacher/password-reset-requests?status=xxx`（教师查看本班申请）
   - `POST /api/teacher/password-reset-requests/{id}/approve`（教师审批）
   - `POST /api/teacher/password-reset-requests/{id}/reject`（教师驳回）
   - 文件：`backend/app/api/v1/routes/admin_routes.py`
   - `GET /api/admin/password-reset-requests?status=xxx`（管理员查看全部）
   - `POST /api/admin/password-reset-requests/{id}/approve`（管理员审批）
   - `POST /api/admin/password-reset-requests/{id}/reject`（管理员驳回）

8. **前端忘记密码弹窗重构**
   - 文件：`frontend/src/views/LoginView.vue`
   - 从单页表单改为三步流程：输入学号 → 有密保则回答密保问题 / 无密保则填留言申请人工重置
   - 超限后弹窗引导一键切换到人工申请
   - 文件：`frontend/src/api/auth.ts` → 新增 `getForgotPasswordQuestions`、`resetPasswordWithAnswers`、`submitPasswordResetRequest`

9. **首次登录改密后引导设置密保**
   - 文件：`frontend/src/views/ChangePasswordView.vue`
   - 修改密码成功后弹出密保设置引导（可跳过，支持 1~3 个自定义问题）

10. **个人中心新增密保问题管理**
    - 文件：`frontend/src/views/ProfileView.vue`
    - 新增"密保问题" Tab：查看已有问题（答案遮蔽）、编辑/新增/删除密保问题

11. **教师端密码重置管理**
    - 文件：`frontend/src/views/teacher/TeacherStudentAdmin.vue`
    - 新增独立卡片"密码重置管理"：状态筛选（待处理/已通过/已驳回）、临时密码显示 + 一键复制、处理人名称、操作时间
    - 文件：`frontend/src/api/teacher.ts` → 新增 `PasswordResetRequest` 类型、`getPasswordResetRequests`、`approvePasswordResetRequest`、`rejectPasswordResetRequest`

12. **管理员端密码重置管理**
    - 新建：`frontend/src/views/admin/AdminPasswordReset.vue`
    - 管理员侧边栏新增"密码重置"入口
    - 文件：`frontend/src/api/admin.ts` → 新增 `getAdminPasswordResetRequests`、`adminApprovePasswordResetRequest`、`adminRejectPasswordResetRequest`
    - 文件：`frontend/src/router/index.ts` → 新增 `/admin/password-reset` 路由

13. **测试种子账号注释**
    - 文件：`backend/seed_data.py`
    - 将 5 个测试种子账号（admin/T001/2025001~2025003）注释，避免生产环境误用

### 验证情况

- `python -m pytest tests/ -v` → **65/66 passed**（1 个预存 Settings monkeypatch 失败，与本轮无关）
- 后端所有新模块导入无误
- 前端 `vue-tsc --noEmit` 类型检查通过
- 密保验证次数限制实测：第 1-4 次错误返回剩余次数，第 5 次返回 429 锁定，第 6 次仍锁定
- 自助重置、人工申请、教师审批、管理员审批流程完整走通

### 遗留问题

- 无

---

## 第 16 轮：课程列表修复 + 多选题支持 + 班级管理增强 + 成绩去重 + 时区修复

### 修改时间

2026-06-03 ~ 2026-06-04

### 修改目标

修复课程列表为空的线上 bug，新增多选题题型，增强班级编辑和学生导入校验，修复学生成绩跨班重复统计和公告时间 8 小时偏移问题。

外部贡献者：mahirushiina05（PR #13）

### 修改内容

1. **课程列表 API 统一返回格式（`658d3ff`）**
   - 文件：`backend/app/api/v1/routes/course_routes.py`、`backend/app/services/course_response_service.py`
   - 问题：教师端 `build_course_list` 返回 `{courses, hint}` 对象，前端期望数组，导致课程列表为空
   - 修复：`build_course_list` 统一返回平铺数组 `Course[]`，补全 `is_owner`/`is_public` 字段
   - 学生端和教师端使用同一返回格式

2. **多选题（multi_choice）支持（`658d3ff`）**
   - 文件：`backend/app/services/quiz_service.py`、`frontend/src/views/PracticeQuizView.vue`、`frontend/src/views/teacher/TeacherQuestions.vue`
   - 后端 `submit_answer` 新增 `multi_choice` 判分逻辑：排序后比对（`sorted(user_answer) == sorted(answer)`）
   - 前端练习页根据题型动态渲染复选框（multi_choice）或单选按钮（choice）
   - 题库管理新增 `multi_choice` 题型选项

3. **管理端公共课程资料上传改造（`658d3ff`）**
   - 文件：`frontend/src/views/admin/AdminPublicCourses.vue`
   - 资料弹窗改为文件上传组件，大小自动计算

4. **班级编辑接口（`6e527ef`）**
   - 文件：`backend/app/services/class_service.py`、`backend/app/api/v1/routes/class_routes.py`
   - 新增 `update_class()` 函数，支持修改班级名称和所属课程
   - 校验同名班级冲突，教师只能编辑自己创建的班级
   - 前端 `TeacherClasses.vue` 新增编辑弹窗

5. **手动添加学生校验增强（`6e527ef`）**
   - 文件：`backend/app/services/class_service.py`、`frontend/src/views/teacher/TeacherStudentAdmin.vue`
   - 新增学号姓名格式校验：已有学生时，输入姓名与系统记录不一致则拒绝
   - 新增 `major`（专业）字段支持
   - 前端表单新增专业输入框

6. **学生成绩跨班去重（`6e527ef`）**
   - 文件：`backend/app/services/teacher_service.py`
   - 问题：同一学生在多个班级时，学生成绩页会重复统计
   - 修复：`list_students` 按学生 ID 去重，多班级合并为一行展示（班级名用顿号分隔）
   - 分页在去重后执行，避免总数不准确

7. **任务概览统计口径修复（`6e527ef`）**
   - 文件：`backend/app/services/task_service.py`
   - 问题：`task_overview` 中 `total_completed`/`total_incomplete` 按任务累加，而非按学生去重
   - 修复：改为按学生维度汇总，学生完成所有分配任务 = 已完成，否则 = 未完成

8. **公告时间存储转 UTC（`6e527ef`）**
   - 文件：`backend/app/services/announcement_service.py`
   - 问题：前端传来北京时间 naive 字符串，直接存入数据库，学生端读取时再偏移 8 小时，导致时间不准确
   - 修复：`create_announcement` 在写入前将北京时间转为 UTC 存储，学生端统一用 `to_beijing_iso` 展示

### 验证情况

- 前端 `npm run build` 通过
- 后端相关测试通过

### 遗留问题

- 无

---

## 第 17 轮：教师端/管理员端/学生端全面优化

### 修改时间

2026-06-04

### 修改目标

全面优化教师端分页搜索、管理员端公共课程管理、学生端答题进度保存和课程搜索。新增跨端通用组件（图片占位、PDF 预览、上传进度）。

### 修改内容

1. **学生端答题进度保存**
   - 新建：`frontend/src/composables/useQuizDraft.ts`
   - 答题中途刷新或离开后，已答题目进度通过 localStorage 恢复
   - `PracticeQuizView.vue` 集成草稿保存和恢复逻辑

2. **学生端课程/资料搜索**
   - 文件：`frontend/src/views/LearnView.vue`、`frontend/src/views/CourseDetailView.vue`
   - 课程列表新增搜索框
   - 课程详情页资料支持搜索

3. **教师端全面分页和搜索**
   - 文件：`frontend/src/views/teacher/TeacherQuestions.vue`、`TeacherMaterials.vue`、`TeacherTaskReport.vue`、`TeacherCourses.vue`
   - 题库管理、资料管理、任务完成报告、课程管理全部新增分页和搜索
   - 后端 `material_service.list_materials` 新增 `keyword`/`page`/`page_size` 参数

4. **教师端资料管理增强**
   - 文件：`backend/app/services/material_service.py`、`backend/app/api/v1/routes/material_routes.py`
   - 新增 `can_view_course_materials()` 学生课程资料权限校验
   - 资料列表支持分页和关键词搜索

5. **学生端答题权限校验**
   - 文件：`backend/app/services/quiz_service.py`
   - `submit_answer` 新增 `role` 参数，学生只能答自己所属课程的题目
   - `get_quiz_stats` 四个统计指标统一限定在学生所属课程范围

6. **管理员端公共课程增强**
   - 文件：`frontend/src/views/admin/AdminPublicCourses.vue`、`backend/app/services/admin_public_course_service.py`
   - 公共课程同步状态展示
   - 资料在线预览

7. **跨端通用组件**
   - 新建：`frontend/src/components/common/AppImage.vue`（图片加载失败占位）
   - 新建：`frontend/src/components/common/PdfPreviewDialog.vue`（PDF 预览弹窗）
   - 新建：`frontend/src/composables/useUploadWithProgress.ts`（上传进度 composable）

8. **其他改进**
   - `TeacherAnnouncements.vue`：发布题目选题交互优化
   - `TeacherStudentAdmin.vue`：学生管理改进
   - `quiz_service.py`：答题后不再写入 StudentProgress（改为实时查询 QuizAttempt）

### 验证情况

- `cd frontend && npm run build` 通过
- 38 个文件变更，+1017/-195 行

### 遗留问题

- 无

---

## 第 18 轮：管理员端增强 + StudentProgress 删除 + 发布题目改造 + 安全增强

### 修改时间

2026-06-05

### 修改目标

删除已废弃的 StudentProgress 模型，管理员端新增公共课程 Excel 批量导入题目和删除教师级联清理，教师端发布题目改为只限自有课程，新增多角色授权函数，删除旧版无认证密码重置接口，清理已完成的设计文档。

### 修改内容

1. **删除 StudentProgress 模型**
   - 文件：`backend/app/models/entities.py`
   - `student_progress` 表和 ORM 模型完全移除
   - `User`/`Course` 模型的 `progress` relationship 删除
   - `quiz_service.py`：答题后不再写入 StudentProgress，`get_course_quiz_stats` 改为实时查询 QuizAttempt
   - `portfolio_service.py`：雷达图「理论基础」从 `learn_progress` 改为答题总量，`study_hours` 改为答题估算
   - `teacher_service.py`：学生列表不再显示 progress/exercises/accuracy
   - `class_service.py`、`question_service.py`：清理学生数据和课程删除检查中移除 StudentProgress

2. **管理员端公共课程 Excel 批量导入题目**
   - 文件：`backend/app/services/admin_public_course_service.py`、`frontend/src/views/admin/AdminPublicCourses.vue`、`frontend/src/api/adminPublicCourse.ts`
   - 管理员可为公共课程批量导入题目（复用 Excel 导入模板）

3. **删除教师级联清理**
   - 文件：`backend/app/api/v1/routes/admin_routes.py`、`frontend/src/views/admin/AdminTeachers.vue`
   - 删除教师时级联清理其所有关联数据（课程、班级、资料、题目、公告等）
   - 新增依赖检查 API，删除前展示关联数据统计

4. **发布题目改为只限自有课程**
   - 文件：`backend/app/services/announcement_service.py`、`frontend/src/views/teacher/TeacherAnnouncements.vue`
   - 后端课程查询从 `or_(created_by == teacher_id, is_public == True)` 收紧为 `created_by == teacher_id`
   - 前端课程下拉框只显示 `is_owner === true` 的课程
   - 新建：`frontend/src/views/teacher/questionSelection.ts`（选题状态同步工具）

5. **多角色授权函数**
   - 文件：`backend/app/core/security.py`
   - 新增 `require_roles(*roles)` 函数，支持同时接受多种角色

6. **删除旧版无认证密码重置接口**
   - 文件：`backend/app/api/v1/routes/auth_routes.py`
   - 移除旧版 `POST /api/password/forgot`（只需学号即可重置密码）

7. **安全修复**
   - 模板下载接口权限扩展到管理员
   - 学生课程答题统计权限校验补全

8. **文档清理**
   - 删除 7 个已完成的设计文档（教师数据隔离、C 组下一步、教师重构概述、课程/班级/发布/审核设计）
   - 新建：`docs/superpowers/plans/2026-06-05-publish-assignment-owned-courses.md`

9. **前端优化**
   - 首页轮播悬停暂停/恢复
   - `IntersectionObserver` 触发后 `unobserve`
   - CSS 颜色注释更新（造→思、练→践、行→悟）

### 验证情况

- 前端 `npm run build` 通过
- 31 个文件变更，+545/-1450 行（大量删除为清理设计文档）

### 遗留问题

- 无

---

## 第 19 轮：学生端学/思页面重构 — 作业精准练习与自由练习分离

### 修改时间

2026-06-06

### 修改目标

重构学生端"思"（练习）页面，将作业练习和自由练习分离为独立入口。作业练习支持按公告题目集精准练习和自动完成标记，自由练习保留按课程练习模式。新增随机练习功能和过期任务标注。

外部贡献者：Lemon（PR #15）

### 修改内容

1. **学/思页面功能分离（`735b68d`）**
   - 文件：`frontend/src/views/LearnView.vue`、`frontend/src/views/PracticeView.vue`
   - "学"页面（LearnView）新增作业任务入口，展示教师发布的公告任务列表
   - "思"页面（PracticeView）重构为自由练习入口，保留按课程练习模式
   - 后端 `course_response_service.py` 为学生端返回关联的公告任务数据

2. **作业精准练习（`735b68d`）**
   - 文件：`frontend/src/views/PracticeQuizView.vue`
   - 新增按公告题目集练习模式：只加载公告指定的 `question_ids`
   - 答题完成后自动标记任务完成（调用后端 `mark_completed`）

3. **随机练习功能（`01162fd`）**
   - 文件：`frontend/src/views/PracticeQuizView.vue`
   - 新增随机打乱题目顺序的练习模式

4. **作业/自由练习分离路由（`01162fd`）**
   - 新建：`frontend/src/views/PracticeAssignments.vue`（218 行）
   - 文件：`frontend/src/router/index.ts`
   - 新增 `/practice/assignments` 路由，独立的作业练习列表页
   - 表格化展示作业列表，标注过期/未开始状态

5. **后端题目接口调整**
   - 文件：`backend/app/api/v1/routes/question_routes.py`、`backend/app/services/announcement_service.py`
   - 题目接口支持按公告 ID 过滤（返回公告指定的题目子集）
   - 公告服务新增题目 ID 列表查询支持

### 验证情况

- 前端 `npm run build` 通过
- 9 个文件变更，+386/-192 行

### 遗留问题

- 无

---

## 第 20 轮：作品提交/审核消息通知系统

### 修改时间

2026-06-06

### 修改目标

新增学生通知系统：作品提交后教师端收到新作品提醒，教师审核（通过/驳回）后学生端收到审核结果通知。

外部贡献者：hanhan（PR #17）

### 修改内容

1. **新增 StudentNotification 模型**
   - 文件：`backend/app/models/entities.py`
   - 新增 `student_notifications` 表（id、user_id、type、title、content、project_id、is_read、created_at）
   - 关联 `User` 和 `Project` 模型

2. **通知服务**
   - 新建：`backend/app/services/notification_service.py`
   - `list_notifications()`：学生通知列表（按时间倒序）
   - `unread_count()`：未读通知计数
   - `mark_read()`：标记已读
   - `create_project_review_notification()`：作品审核结果推送（通过/驳回 + 原因）

3. **通知 API 路由**
   - 新建：`backend/app/api/v1/routes/notification_routes.py`
   - `GET /api/notifications`：学生通知列表
   - `GET /api/notifications/unread-count`：未读计数
   - `POST /api/notifications/{id}/read`：标记已读
   - 在 `__init__.py` 注册 `notification_router`

4. **作品审核触发通知**
   - 文件：`backend/app/services/project_service.py`
   - 教师审核通过或驳回时自动调用 `create_project_review_notification`

5. **前端通知展示**
   - 新建：`frontend/src/api/notification.ts`
   - 文件：`frontend/src/components/AppHeader.vue`、`frontend/src/components/AnnouncementPopup.vue`、`frontend/src/views/InboxView.vue`
   - 顶部导航新增未读通知角标
   - 收件箱页展示通知列表（区分公告任务和审核通知）
   - 教师端布局新增新作品提醒

6. **教师端新作品提示**
   - 文件：`frontend/src/views/teacher/TeacherLayout.vue`
   - 教师侧边栏或顶部新增待审核作品数量提示

### 验证情况

- 12 个文件变更，+361/-15 行

### 遗留问题

- `student_notifications` 表在 `schema_compat.py` 中的自动创建逻辑需确认

---

## 第 21 轮：前端样式和交互微调

### 修改时间

2026-06-06

### 修改目标

微调首页展示、练习答题交互和教师任务报告样式。

### 修改内容

1. **首页模块展示优化**
   - 文件：`frontend/src/components/home/ModuleShowcase.vue`、`frontend/src/views/HomeView.vue`
   - 模块展示卡片样式调整
   - 首页布局微调

2. **练习答题交互改进**
   - 文件：`frontend/src/views/PracticeQuizView.vue`
   - 答题交互细节优化（选项反馈、提交确认等）

3. **教师任务报告样式**
   - 文件：`frontend/src/views/teacher/TeacherTaskReport.vue`
   - 表格列宽和布局微调

4. **全局样式**
   - 文件：`frontend/src/assets/main.css`
   - 颜色和间距微调

### 验证情况

- 5 个文件变更，+96/-35 行

### 遗留问题

- 无

---

## 第 22 轮：任务完成报告增强 + 公告题目任务闭环 + 消息中心体验优化

### 修改时间

2026-06-07

### 修改目标

1. 增强教师端任务完成报告，补充教师姓名、目标人数、按题去重计分等统计信息
2. 补齐公告题目任务练习闭环：学生端可通过任务精确跳转到对应题目集练习，答题记录关联任务维度
3. 修复消息中心（收件箱）体验问题：通知链接正确跳转、公告弹窗自动获取最新通知、顶部角标实时刷新

### 修改内容

**一、教师端任务完成报告增强**

1. **任务完成报告新增统计字段**
   - 文件：`backend/app/services/task_service.py`
   - `completion_report` 新增 `teacher_name`（教师姓名）、`target_count`（目标学生人数）、`completion_rate`（完成率）
   - `total_score` 计算改为按题去重（每题只计最新一次答题），解决重复答题超过 100 分的问题
   - `_is_expired` 提取为模块级函数，避免内部重复定义

2. **任务概览统计增强**
   - 文件：`backend/app/services/task_service.py`
   - `task_overview` 新增教师姓名和目标人数统计
   - 完成率计算基于目标人数

3. **前端成绩导出功能**
   - 文件：`frontend/src/views/teacher/TeacherTaskReport.vue`
   - 新增 Excel 导出按钮，复用教师学生管理页导出能力

**二、公告题目任务练习闭环**

4. **新增「获取任务题目」后端接口**
   - 文件：`backend/app/api/v1/routes/announcement_routes.py`
   - 新增 `GET /api/announcements/{id}/questions`（学生端）
   - 校验学生班级归属、任务开始时间、截止时间
   - 返回公告信息 + 指定题目列表（含课程名、题型、选项、答案、解析）

5. **新增「获取任务题目」服务函数**
   - 文件：`backend/app/services/task_service.py`
   - 新增 `get_assignment_questions()` 函数
   - 集成任务权限校验（班级归属、开始/截止时间、题目过滤）

6. **答题记录关联任务维度**
   - 文件：`backend/app/models/entities.py`
   - `QuizAttempt` 新增 `announcement_id` 字段（可选，关联题目任务 ID）
   - 文件：`backend/app/schemas/common.py`
   - `SubmitAnswerRequest` 新增 `announcement_id` 可选字段
   - 文件：`backend/app/api/v1/routes/quiz_routes.py`
   - `submit_answer` 传递 `announcement_id` 参数
   - 文件：`backend/app/services/quiz_service.py`
   - `submit_answer` 写入 `announcement_id` 到 QuizAttempt

7. **数据库 Schema 兼容**
   - 文件：`backend/app/db/schema_compat.py`
   - `_ensure_column` 新增 `quiz_attempts.announcement_id` 列自动创建
   - 文件：`backend/tests/test_schema_compat.py`
   - 新增 `announcement_id` 兼容列测试

8. **前端任务练习页重构**
   - 文件：`frontend/src/views/PracticeQuizView.vue`
   - 新增 `announcementId` 路由参数支持
   - 有 `announcementId` 时调用 `/announcements/{id}/questions` 获取题目
   - 答题提交时携带 `announcement_id` 关联任务
   - 随机练习按钮文案改为"随机练习"

9. **前端任务题目 API**
   - 文件：`frontend/src/api/announcement.ts`
   - 新增 `getAssignmentQuestions()` 函数
   - 文件：`frontend/src/api/quiz.ts`
   - `SubmitAnswerRequest` 新增 `announcement_id` 可选字段

10. **前端新增任务练习路由**
    - 文件：`frontend/src/router/index.ts`
    - 新增 `/practice/announcement/:announcementId` 路由，组件复用 `PracticeQuizView`

**三、消息中心体验优化**

11. **收件箱任务链接修复**
    - 文件：`frontend/src/views/InboxView.vue`
    - 题目任务的"去练习"按钮链接改为 `/practice/announcement/:id`，跳转到对应任务题目集
    - 新增跳转函数，区分任务类型和作品通知的不同跳转目标

12. **公告弹窗自动获取最新通知**
    - 文件：`frontend/src/components/AnnouncementPopup.vue`
    - 弹窗显示时自动调用通知列表 API 获取最新一条通知（补充公告轮询遗漏的审核通知）

13. **顶部未读角标实时刷新**
    - 新建：`frontend/src/utils/messageRefresh.ts`
    - 提供 `emitMessageRefresh()` / `onMessageRefresh()` 全局事件机制
    - 文件：`frontend/src/components/AppHeader.vue`
    - 监听消息刷新事件，实时更新未读角标
    - 文件：`frontend/src/views/InboxView.vue`
    - 标记已读后触发 `emitMessageRefresh()` 同步头部角标
    - 文件：`frontend/src/components/AnnouncementPopup.vue`
    - 标记已读后触发 `emitMessageRefresh()` 同步头部角标

**四、项目地图更新**

14. **project-map.md 更新**
    - 文件：`docs/superpowers/project-map.md`
    - 新增 `Announcement → QuizAttempt(announcement_id) → TaskCompletion` 数据关系
    - 新增 `/practice/announcement/:announcementId` 路由
    - 新增题目任务练习的 `announcement_id` 关联约定

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/models/entities.py` | 修改（QuizAttempt 新增 announcement_id） |
| `backend/app/schemas/common.py` | 修改（SubmitAnswerRequest 新增 announcement_id） |
| `backend/app/services/task_service.py` | 修改（completion_report 增强 + get_assignment_questions） |
| `backend/app/services/quiz_service.py` | 修改（submit_answer 写入 announcement_id） |
| `backend/app/api/v1/routes/announcement_routes.py` | 修改（新增 /questions 端点） |
| `backend/app/api/v1/routes/quiz_routes.py` | 修改（传递 announcement_id） |
| `backend/app/db/schema_compat.py` | 修改（quiz_attempts.announcement_id 兼容列） |
| `backend/tests/test_schema_compat.py` | 修改（新增测试） |
| `backend/tests/test_integration_bugfixes.py` | 修改（新增集成测试） |
| `backend/tests/test_assignment_practice_flow.py` | 新建（任务练习流程测试） |
| `frontend/src/api/announcement.ts` | 修改（新增 getAssignmentQuestions） |
| `frontend/src/api/quiz.ts` | 修改（SubmitAnswerRequest 新增 announcement_id） |
| `frontend/src/views/PracticeQuizView.vue` | 修改（支持任务练习模式） |
| `frontend/src/views/InboxView.vue` | 修改（通知跳转修复 + 角标刷新） |
| `frontend/src/components/AnnouncementPopup.vue` | 修改（自动获取最新通知 + 角标刷新） |
| `frontend/src/components/AppHeader.vue` | 修改（监听消息刷新事件） |
| `frontend/src/router/index.ts` | 修改（新增任务练习路由） |
| `frontend/src/utils/messageRefresh.ts` | 新建（消息刷新事件工具） |
| `frontend/index.html` | 修改（标题文案微调） |
| `frontend/package.json` | 修改（项目名微调） |
| `frontend/README.md` | 修改（精简） |
| `backend/README.md` | 修改（测试命令说明） |
| `docs/superpowers/project-map.md` | 修改（数据关系和路由更新） |

### 验证情况

- 前端 `npm run type-check` 通过
- 后端 `py -m pytest tests/ -v` 通过
- 任务完成报告新增字段（教师姓名、目标人数、完成率）正确返回
- 按题去重计分后分数不会超过 100
- 学生端任务练习可跳转到对应题目集
- 答题记录正确携带 `announcement_id`
- 未读角标在标记已读后实时刷新

### 遗留问题

- 无

---

## 第 23 轮：课程详情学生权限收尾 + 项目状态同步

### 修改时间

2026-06-24

### 修改目标

收紧学生访问课程详情的权限边界，合并本轮修改记录到唯一总记录文件，并把项目状态从旧清单同步到当前代码事实。

### 修改内容

1. **课程详情学生权限校验**
   - 文件：`backend/app/api/v1/routes/course_routes.py`
   - 学生访问 `/api/courses/{course_id}` 时先校验是否已加入该课程，未加入时返回 `课程不存在`。
   - 文件：`backend/app/api/v1/routes/question_routes.py`
   - 兼容入口 `/api/questions/courses/{course_id}` 使用同样的学生课程访问校验。

2. **课程详情权限回归测试**
   - 文件：`backend/tests/test_student_course_visibility.py`
   - 新增学生不能访问未加入课程详情的回归测试，覆盖两个课程详情入口。

3. **教师端作业完成静态测试同步**
   - 文件：`frontend/src/views/teacher/TeacherTaskReport.vue`
   - 保留 `/teacher/task-report?task_id=...` 跳转到任务完成详情的入口行为。
   - 文件：`frontend/tests/teacher-task-report-migration-static.test.mjs`
   - 将函数名断言改为行为级断言，匹配详情跳转函数和详情路由名。

4. **项目状态同步**
   - 文件：`AGENTS.md`、`CLAUDE.md`
   - 标记教师端学生成绩作业分数展示已完成。
   - 标记课程详情学生权限和 `mark_completed` 校验已完成或已核实。
   - 明确后续修改记录统一追加到 `backend/docs/项目修改记录.md`，不再按日期新建分散记录文件。

5. **本地输出目录忽略**
   - 文件：`.gitignore`
   - 保留 `output/` 忽略规则，避免本地生成输出进入版本管理。

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/api/v1/routes/course_routes.py` | 修改（课程详情学生权限校验） |
| `backend/app/api/v1/routes/question_routes.py` | 修改（兼容课程详情入口权限校验） |
| `backend/tests/test_student_course_visibility.py` | 修改（新增回归测试） |
| `frontend/src/views/teacher/TeacherTaskReport.vue` | 修改（恢复 task_id 查询参数跳转详情） |
| `frontend/tests/teacher-task-report-migration-static.test.mjs` | 修改（静态断言改为行为级） |
| `.gitignore` | 修改（忽略 output/ 本地输出目录） |
| `AGENTS.md` | 修改（同步当前优先级和记录规范） |
| `CLAUDE.md` | 修改（同步修改记录入口说明） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮记录和单文件记录规范） |

### 验证情况

- `backend\.venv\Scripts\pytest.exe backend\tests\test_auth.py backend\tests\test_student_course_visibility.py backend\tests\test_assignment_practice_flow.py -q` 通过：31 passed, 1 warning
- `PYTEST_ADDOPTS="--basetemp C:\Users\ASUS\AppData\Local\Temp\pytest-codex-tongshi" backend\.venv\Scripts\pytest.exe backend\tests -q` 通过：139 passed, 1 warning
- `node frontend/tests/teacher-task-report-migration-static.test.mjs` 通过
- `cmd /c npm run build` 通过，仍有 Vite chunk size warning

### 遗留问题

- `fastapi.testclient` 仍有 `StarletteDeprecationWarning`，不影响本轮修复。

---

## 第 24 轮：服务器部署影响说明规则补充

### 修改时间

2026-06-24

### 修改目标

补充长期协作规则：项目已经部署到服务器，后续每次修改完成后都必须明确说明服务器侧需要同步执行哪些操作。

### 修改内容

1. **项目规则补充服务器部署影响说明**
   - 文件：`AGENTS.md`
   - 在文档同步和验收底线中增加服务器部署影响说明要求。
   - 每次总结必须说明服务器是否需要拉代码、重启服务、重新构建前端、执行数据库迁移、调整环境变量或修改配置。
   - 即使不需要服务器操作，也必须明确写明“不需要服务器修改”。

2. **本地协作记忆同步**
   - 文件：`CLAUDE.md`
   - 修改项目修改记录说明，补充服务器部署影响说明要求。

3. **总修改记录模板更新**
   - 文件：`backend/docs/项目修改记录.md`
   - 维护建议模板新增“服务器部署影响”栏目，后续修改记录必须填写。

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `AGENTS.md` | 修改（新增服务器部署影响说明规则） |
| `CLAUDE.md` | 修改（同步服务器部署影响说明要求） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮记录并更新维护模板） |

### 验证情况

- `git diff --check` 通过

### 服务器部署影响

- 本轮只修改项目规则和修改记录文档，不需要服务器拉代码、重启服务、重新构建前端、执行数据库迁移或调整配置。

### 遗留问题

- 无

---

## 第 25 轮：课程阶段化改造联动缺陷修复

### 修改时间

2026-06-25

### 修改目标

修复课程阶段化改造后三端（管理员/教师/学生）联动中发现的 8 项功能缺陷，包括 API 模板字符串错误、不存在的属性引用、stage_id 无法清空、阶段接口权限缺失、事务控制不当等问题。

### 修改内容

1. **修复 adminPublicCourse.ts 16 处 URL 模板字符串缺失反引号和插值**
   - 文件：`frontend/src/api/adminPublicCourse.ts`
   - 问题：所有动态 URL 使用普通字符串而非模板字符串，导致 `${courseId}` 等变量未被解析，请求地址错误
   - 修复：全部改为反引号模板字符串，共修复 16 处

2. **修复 course_response_service.py 中 `material.is_synced` 不存在属性**
   - 文件：`backend/app/services/course_response_service.py`
   - 问题：`_format_material` 使用 `material.is_synced`，但 `Material` 模型没有该属性
   - 修复：改为 `bool(material.source_material_id)`

3. **修复 course_response_service.py 阶段数据补充 source_stage_id 和排序稳定性**
   - 文件：`backend/app/services/course_response_service.py`
   - 修复：`build_course_detail` 中 stage_data 补充 `source_stage_id` 字段；排序键改为 `(sort_order, id)` 保证稳定性

4. **修复 stage_id 无法通过 API 显式清空为 null**
   - 文件：`backend/app/services/material_service.py`、`backend/app/api/v1/routes/material_routes.py`
   - 问题：Pydantic `Optional[int] = None` 无法区分"未传"和"显式 null"
   - 修复：`update_material` 新增 `clear_stage_id` 参数；路由层通过 `model_fields_set` 判断是否显式传 null

5. **修复管理员删除公共课程时未清理 source_stage_id**
   - 文件：`backend/app/services/admin_public_course_service.py`
   - 问题：`delete_public_course` 清理了同步资料和题目，但遗漏了 `source_stage_id` 引用
   - 修复：新增清理查询，将副本阶段的 `source_stage_id` 置 null

6. **修复课程阶段接口缺少学生访问权限校验**
   - 文件：`backend/app/api/v1/routes/course_routes.py`
   - 问题：`get_course_stages` 对学生无 enrolled 检查，学生可直接访问未加入课程的阶段
   - 修复：增加 `student_can_access_course` 校验

7. **修复 course_stage_service 中过早 commit 导致路由层无法控制事务**
   - 文件：`backend/app/services/course_stage_service.py`、`backend/app/api/v1/routes/stage_routes.py`
   - 问题：服务层直接 `db.commit()`，路由层无法追加后续操作或统一回滚
   - 修复：服务层改为 `db.flush()`，路由层负责 `db.commit()`

8. **修复管理员编辑阶段名称未 strip 空格**
   - 文件：`backend/app/api/v1/routes/admin_public_course_routes.py`、`backend/app/api/v1/routes/stage_routes.py`
   - 修复：对 `data.name` 添加 `.strip()` 处理

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `frontend/src/api/adminPublicCourse.ts` | 修改（修复 16 处 URL 模板字符串） |
| `backend/app/services/course_response_service.py` | 修改（is_synced、source_stage_id、排序） |
| `backend/app/services/material_service.py` | 修改（clear_stage_id 参数） |
| `backend/app/api/v1/routes/material_routes.py` | 修改（model_fields_set 判断） |
| `backend/app/services/admin_public_course_service.py` | 修改（source_stage_id 清理） |
| `backend/app/api/v1/routes/course_routes.py` | 修改（学生访问校验） |
| `backend/app/services/course_stage_service.py` | 修改（commit 改 flush） |
| `backend/app/api/v1/routes/stage_routes.py` | 修改（commit + strip） |
| `backend/app/api/v1/routes/admin_public_course_routes.py` | 修改（strip） |

### 验证情况

- `npm run build` 通过（含 vue-tsc type-check），构建产物正常输出到 dist/
- 后端 143 项测试通过

### 服务器部署影响

- 需要拉取代码并重启后端服务（涉及路由和服务层改动）
- 需要重新构建前端（adminPublicCourse.ts 修复）
- 不需要执行数据库迁移（无表结构变更，schema_compat 自动迁移已在第 24 轮就绪）
- 不需要修改环境变量或 Nginx 配置

### 遗留问题

- 无

---

## 第 26 轮：课程阶段化联动深度审查修复

### 修改时间

2026-06-25

### 修改目标

对第 25 轮修复后的代码进行第二轮深度审查，修复新发现的 6 项高优先级问题，包括管理员阶段操作权限越界、公共资料 stage_id 无校验、表单嵌套错误、前端错误处理缺失、题库管理 Tab 缺失和课程详情页滚动监听缺失。

### 修改内容

1. **修复管理员操作阶段时 teacher_id 权限越界**
   - 文件：`backend/app/api/v1/routes/admin_public_course_routes.py`
   - 问题：管理员创建/删除阶段时传入 `current_user.id` 作为 `teacher_id`，导致管理员无法操作非自己创建的阶段
   - 修复：管理员路由中移除 teacher_id 参数

2. **修复管理员新增/修改公共资料时 stage_id 无归属校验**
   - 文件：`backend/app/services/admin_public_course_service.py`
   - 问题：创建或修改公共资料时未校验 stage_id 是否属于目标课程，可传入任意课程阶段
   - 修复：新增 `_validate_stage_id` 辅助函数，在 `create_public_material` 和 `update_public_material` 中调用

3. **修复管理员资料修改时 stage_id 无法设为 null + update 使用 exclude_unset**
   - 文件：`backend/app/services/admin_public_course_service.py`、`backend/app/api/v1/routes/admin_public_course_routes.py`
   - 问题：update 循环跳过所有 None 值，无法显式清空 stage_id；且未使用 `exclude_unset=True`
   - 修复：循环允许 stage_id 为 None 时赋值；路由改为 `model_dump(exclude_unset=True)`

4. **修复 AdminPublicCourses.vue 资料表单 el-form-item 嵌套错误**
   - 文件：`frontend/src/views/admin/AdminPublicCourses.vue`
   - 问题："资料标题" 的 `el-form-item` 未正确关闭，导致"所属阶段"被嵌套在内部
   - 修复：重构为正确的并列结构

5. **修复 AdminPublicCourses.vue 题库管理 Tab 缺失**
   - 文件：`frontend/src/views/admin/AdminPublicCourses.vue`
   - 问题：题目 CRUD 函数和弹窗代码已存在，但 `<el-tabs>` 中缺少"题库管理"`<el-tab-pane>`，UI 无法访问
   - 修复：新增"题库管理"Tab，包含新增/批量导入工具栏和题目列表表格

6. **修复 CourseDetailView.vue 侧边栏滚动高亮不跟随**
   - 文件：`frontend/src/views/CourseDetailView.vue`
   - 问题：用户手动滚动时，左侧目录的 active 状态不会更新
   - 修复：添加 IntersectionObserver 监听所有 `.stage-section`，进入视口时自动更新 `activeStageId`；`onUnmounted` 清理观察器

7. **补充 CourseDetailView.vue 错误处理**
   - 文件：`frontend/src/views/CourseDetailView.vue`
   - 问题：`loadCourse` 的 try/catch 缺少错误提示
   - 修复：catch 中添加 `ElMessage.error` 提示用户网络异常

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/api/v1/routes/admin_public_course_routes.py` | 修改（teacher_id 移除、exclude_unset） |
| `backend/app/services/admin_public_course_service.py` | 修改（_validate_stage_id、stage_id null 处理） |
| `frontend/src/views/admin/AdminPublicCourses.vue` | 修改（表单嵌套修复、题库管理 Tab） |
| `frontend/src/views/CourseDetailView.vue` | 修改（IntersectionObserver、错误处理） |

### 验证情况

- `npm run build` 通过（含 vue-tsc type-check），构建产物正常输出到 dist/
- 后端 143 项测试全部通过

### 服务器部署影响

- 需要拉取代码并重启后端服务（涉及路由和服务层改动）
- 需要重新构建前端（AdminPublicCourses.vue 和 CourseDetailView.vue 改动）
- 不需要执行数据库迁移（无表结构变更）
- 不需要修改环境变量或 Nginx 配置

### 遗留问题

- 无

---

## 第 27 轮：三端联动深度审查与数据完整性加固

### 修改时间

2026-06-25

### 修改目标

对三端联动进行第三轮深度代码审查，修复 15 项高/中/低优先级问题，涵盖数据一致性、事务安全、前端交互体验和数据库迁移完整性。

### 修改内容

1. **修复编辑公共题目时 model_dump 缺少 exclude_unset（高）**
   - 文件：`backend/app/api/v1/routes/admin_public_course_routes.py`
   - 问题：`edit_public_question` 使用 `data.model_dump()` 输出全部字段（含默认值），导致部分更新时未传字段被默认值覆盖（如 options=[]、tags=[]），且同步传播到教师副本
   - 修复：改为 `data.model_dump(exclude_unset=True)`

2. **修复批量导入题目 flush 失败后 Session 损坏（高）**
   - 文件：`backend/app/services/admin_public_course_service.py`
   - 问题：`import_questions_to_public_course` 循环中 `db.flush()` 失败后 Session 进入无效状态，后续行全部失败，已成功行也被回滚
   - 修复：每行使用 `db.begin_nested()` 保存点隔离，失败时仅回滚该行

3. **修复教师端创建阶段缺少 db.commit()（高）**
   - 文件：`backend/app/api/v1/routes/course_routes.py`
   - 问题：`add_course_stage` 调用 `create_stage`（内部仅 flush），路由层未 commit，请求结束后 Session 关闭导致隐式回滚，新建阶段丢失
   - 修复：`create_stage()` 后补 `db.commit()` + `db.refresh(stage)`

4. **修复管理员编辑阶段时 course_id 归属校验在 flush 之后（高）**
   - 文件：`backend/app/api/v1/routes/admin_public_course_routes.py`
   - 问题：`edit_public_stage` 先调用 `update_stage`（内部 flush 已写入修改），后才检查 `stage.course_id != course_id`，导致数据不一致
   - 修复：调用 `update_stage` 之前先查询并验证阶段归属

5. **修复删除公共阶段后未清理教师副本中的对应阶段（中）**
   - 文件：`backend/app/api/v1/routes/admin_public_course_routes.py`
   - 问题：`remove_public_stage` 删除源阶段后调用 `sync_stages_to_course_copies`，但该函数只做创建+更新不做删除，副本中残留孤儿阶段
   - 修复：删除源阶段前，先查找并删除所有 `source_stage_id` 引用该阶段的副本阶段（副本阶段下有资料时将其设为未分类）

6. **schema_compat 补建 showcase_item_images 表（中）**
   - 文件：`backend/app/db/schema_compat.py`
   - 问题：ORM 模型 `ShowcaseItemImage` 存在但迁移脚本未创建该表，旧库升级后访问图文内容图片时报 500
   - 修复：在 `showcase_items` 表创建后增加 `showcase_item_images` 表创建逻辑（含 FK 和索引）

7. **schema_compat 补建 source_stage_id FK 约束和索引（中）**
   - 文件：`backend/app/db/schema_compat.py`
   - 问题：迁移创建的 `course_stages` 表缺少 `source_stage_id` 外键约束和索引，同步服务依赖此字段查询
   - 修复：添加 `ix_course_stages_source_stage_id` 索引和 `fk_course_stages_source_stage_id` 外键（ON DELETE SET NULL）

8. **学生端课程详情资料链接空值防护（中）**
   - 文件：`frontend/src/views/CourseDetailView.vue`
   - 问题：资料 `file_id` 和 `url` 均为空时，`<a href="">` 导致页面自跳转
   - 修复：`<a>` 改为 `<div @click="openMaterial(...)">`，`openMaterial` 函数检查空 URL 并弹出提示

9. **教师端上传资料错误提示区分（中）**
   - 文件：`frontend/src/views/teacher/TeacherCourseDetail.vue`
   - 问题：文件上传成功后 `createMaterial` 失败时，统一显示"上传失败"，误导用户重新上传
   - 修复：分拆 try/catch，分别提示"文件上传失败"和"资料记录创建失败，文件已上传"

10. **学生端侧边栏添加未分类资料导航入口（中）**
    - 文件：`frontend/src/views/CourseDetailView.vue`
    - 问题：侧边栏仅显示阶段列表，有未分类资料时无导航入口
    - 修复：在阶段列表末尾添加"未分类资料"toc-item，点击滚动至对应区域

11. **Material.stage_id FK 添加 ondelete="SET NULL"（低）**
    - 文件：`backend/app/models/entities.py`
    - 修复：阶段删除时资料自动回到未分类状态，避免 FK 约束阻止删除

12. **update 循环排除敏感字段（低）**
    - 文件：`backend/app/services/admin_public_course_service.py`
    - 修复：`update_public_question` 和 `update_public_material` 的循环中增加 `_PROTECTED_FIELDS` 集合，排除 id、course_id、source_*_id、created_at

13. **移除 AdminPublicCourses.vue 中未使用的 selectedCourseId（低）**
    - 文件：`frontend/src/views/admin/AdminPublicCourses.vue`
    - 修复：删除死代码

14. **公共课程 description 字段添加 strip 处理（低）**
    - 文件：`backend/app/services/admin_public_course_service.py`
    - 修复：`create_public_course` 和 `update_public_course` 中对 description 调用 `.strip()`

15. **IntersectionObserver forEach 非空断言改进（低）**
    - 文件：`frontend/src/views/CourseDetailView.vue`
    - 修复：`observer.value!.observe()` 改为先捕获局部变量再遍历，避免极端情况下的空引用

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/api/v1/routes/admin_public_course_routes.py` | 修改（exclude_unset、归属校验、阶段清理、Material 导入） |
| `backend/app/api/v1/routes/course_routes.py` | 修改（add_course_stage 补 commit） |
| `backend/app/services/admin_public_course_service.py` | 修改（begin_nested、_PROTECTED_FIELDS、strip） |
| `backend/app/models/entities.py` | 修改（Material.stage_id ondelete） |
| `backend/app/db/schema_compat.py` | 修改（showcase_item_images 表、source_stage_id FK/索引） |
| `frontend/src/views/CourseDetailView.vue` | 修改（openMaterial、侧边栏、IntersectionObserver） |
| `frontend/src/views/teacher/TeacherCourseDetail.vue` | 修改（上传/创建错误提示分离） |
| `frontend/src/views/admin/AdminPublicCourses.vue` | 修改（移除死代码） |

### 验证情况

- `npm run build` 通过（含 vue-tsc type-check），构建产物正常输出到 dist/
- 后端 143 项测试全部通过

### 服务器部署影响

- 需要拉取代码并重启后端服务（路由、服务、模型层均有改动）
- 需要重新构建前端（3 个 Vue 组件改动）
- schema_compat 会自动补建 showcase_item_images 表和 source_stage_id 索引/FK，无需手动数据库迁移
- 不需要修改环境变量或 Nginx 配置

### 遗留问题

- 无

---

## 第 28 轮：MySQL TEXT 默认值启动失败热修复

### 修改时间

2026-06-27

### 修改目标

修复服务器更新后后端启动失败的问题。线上 MySQL 在执行 `schema_compat.py` 自动补齐 `courses.description` 字段时，拒绝 `TEXT DEFAULT ''`，导致 `tongshi-backend` 反复退出。

### 修改内容

1. **修复 `courses.description` 兼容补列 SQL**
   - 文件：`backend/app/db/schema_compat.py`
   - 问题：MySQL 不允许 `TEXT` 类型字段设置默认值，原 SQL `ALTER TABLE courses ADD COLUMN description TEXT DEFAULT ''` 会触发 `(1101, "BLOB, TEXT, GEOMETRY or JSON column 'description' can't have a default value")`
   - 修复：改为 `ALTER TABLE courses ADD COLUMN description TEXT NULL`，保持字段可空，由业务层按需处理空值

2. **补充 MySQL 兼容回归测试**
   - 文件：`backend/tests/test_schema_compat.py`
   - 新增测试模拟 MySQL 旧库缺少 `courses.description` 时的兼容脚本输出，确保不会再次生成 `TEXT DEFAULT ''`

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/db/schema_compat.py` | 修改（修复 MySQL TEXT 补列 SQL） |
| `backend/tests/test_schema_compat.py` | 修改（新增回归测试） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮热修复记录） |

### 验证情况

- `python -m pytest backend\tests\test_schema_compat.py::test_ensure_schema_compatibility_adds_course_description_without_mysql_text_default -q` 通过
- `python -m pytest backend\tests\test_schema_compat.py -q` 通过：14 passed

### 服务器部署影响

- 需要拉取代码并重启后端服务，后端启动时会自动用 `TEXT NULL` 补齐 `courses.description`
- 不需要重新构建前端（无前端文件改动）
- 不需要手动数据库迁移；如果服务器尚未成功添加 `courses.description`，本次代码会在启动时自动补齐
- 不需要修改环境变量或 Nginx 配置

### 遗留问题

- 无

---

## 第 29 轮：教师端题目选择弹窗右侧已选题目横向展示修复

### 修改时间

2026-06-27

### 修改目标

修复教师端发布题目弹窗里右侧“已选题目”列表在窄宽度下竖排、挤压和乱码式换行的问题，让题型标签、标签列表和题干横向可读，避免题目内容被压成竖向显示。

### 修改内容

1. **调整右侧已选题目条目结构**
   - 文件：`frontend/src/views/teacher/TeacherAnnouncements.vue`
   - 将题型标签与题目标签单独放入一行，题干单独展示为一行省略文本，避免同一行内内容互相挤压

2. **优化右侧面板和条目布局**
   - 文件：`frontend/src/views/teacher/TeacherAnnouncements.vue`
   - 适当加宽右侧选择栏，并补充 `min-width: 0`、单行省略、窄屏下按钮换行等规则，防止中文题干被压成竖排

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `frontend/src/views/teacher/TeacherAnnouncements.vue` | 修改（右侧已选题目横向展示） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮记录） |

### 验证情况

- 待执行：`frontend` 下 `npm run build`
- 待执行：浏览器打开题目选择弹窗，确认右侧已选题目横向展示且无乱码竖排

### 服务器部署影响

- 需要拉取代码并重新构建前端，然后重启前端静态服务或前端容器
- 后端不需要修改
- 不需要数据库迁移
- 不需要修改环境变量或 Nginx 配置

### 遗留问题

- 无

---

## 第 30 轮：代码清理与搜索优化

### 修改时间

2026-06-30

### 修改目标

在不打断教师端课程、题库、资料和学生学习入口的前提下，迁移废弃课程兼容接口，清理确认无引用的代码，并把课程/资料/题目/学生列表的关键搜索与分页逻辑下沉到数据库层。

### 修改内容

1. **课程正式入口统一**
   - 前端 `addPublicCourse()` 从 `/questions/courses/{id}/add` 改为 `/courses/{id}/add`。
   - 后端测试全部从 `/api/questions/courses*` 迁移到 `/api/courses*`。
   - 删除 `backend/app/api/v1/routes/question_routes.py` 中的六个兼容课程 endpoint 及相关 import。
   - `course_routes.py` 顶部注释由"旧路由仍保留"改为"兼容路由已删除"说明。

2. **课程列表数据库分页**
   - 新增 `build_course_page()` 在数据库层完成过滤、计数和 limit/offset，替代原 Python 切片方案。
   - 新增 `_format_course_batch()` 批量预计算资料数、班级数和题目总数，减少 N+1 查询。
   - 文件：`backend/app/services/course_response_service.py`（全面重构）、`backend/app/api/v1/routes/course_routes.py`。
   - 测试：`backend/tests/test_course_pagination.py`（2 个测试）。

3. **搜索语义统一为 `ilike()`**
   - 课程名称、题目题干、资料标题搜索从 `contains()` 改为 `ilike()`。
   - 文件：`backend/app/services/question_service.py`、`backend/app/services/material_service.py`。
   - 测试：`backend/tests/test_search_optimization.py`（4 个测试：大小写和中文子串）。

4. **教师学生列表分页下沉数据库**
   - `list_students()` 用 SQLAlchemy `subquery` + `group_by` 实现数据库去重，再用 offset/limit 分页。
   - 关键词搜索改为 `ilike()`。
   - 文件：`backend/app/services/teacher_service.py`。
   - 测试：`backend/tests/test_teacher_students_pagination.py`（2 个测试）。

5. **题目查重候选集优化**
   - `find_duplicate_question()` 增加 `_candidate_stem_fragment()`，先按题型和候选片段缩小扫描范围，再对候选集做指纹比对。
   - 文件：`backend/app/services/question_bank_service.py`。
   - 测试：`backend/tests/test_question_duplicate_candidates.py`（2 个测试）。

6. **删除废弃代码**
   - 删除 `ActivityEvent` ORM 模型，不做数据库迁移。
   - 删除前端未使用的 `deleteQuestion` 和 `batchDeleteQuestions` API 导出。

7. **教师课程管理主列表使用后端分页**
   - `TeacherCourses.vue` 废弃全量 `getCourses()` + 前端过滤模式，改为 owned/public 两组独立后端分页。
   - 添加公共课程弹窗改用 `getCoursesPage({scope:'public'})` 加载。

8. **学生学习页搜索传 keyword 给后端**
   - `LearnView.vue` 的 `loadCourses()` 把 `keyword` 传给 `getCourseList()`。

9. **远程搜索输入添加防抖**
   - 新增 `frontend/src/composables/useDebounce.ts`（300ms）。
   - 教师端题目、资料、作品审核关键词搜索改为防抖模式。

10. **BOM 字符清理**
    - 批量移除 6 个 Python 文件的 `U+FEFF` BOM 头。

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/api/v1/routes/course_routes.py` | 修改（路由改用 build_course_page、注释） |
| `backend/app/services/course_response_service.py` | 重写（新增分页服务、批量格式化） |
| `backend/app/services/question_service.py` | 修改（contains → ilike 两处） |
| `backend/app/services/material_service.py` | 修改（contains → ilike） |
| `backend/app/services/teacher_service.py` | 重写（list_students 数据库分页） |
| `backend/app/services/question_bank_service.py` | 修改（find_duplicate_question 候选集优化） |
| `backend/app/api/v1/routes/question_routes.py` | 修改（删除兼容路由及 import） |
| `backend/app/models/entities.py` | 修改（删除 ActivityEvent 模型） |
| `frontend/src/api/course.ts` | 修改（addPublicCourse 路径） |
| `frontend/src/api/question.ts` | 修改（删除未用导出） |
| `frontend/src/composables/useDebounce.ts` | 新建（防抖 composable） |
| `frontend/src/views/teacher/TeacherCourses.vue` | 重写（后端分页） |
| `frontend/src/views/teacher/TeacherQuestions.vue` | 修改（防抖） |
| `frontend/src/views/teacher/TeacherMaterials.vue` | 修改（防抖） |
| `frontend/src/views/teacher/TeacherReviews.vue` | 修改（防抖） |
| `frontend/src/views/LearnView.vue` | 修改（keyword 传给后端） |
| `backend/tests/test_course_pagination.py` | 新建（2 个测试） |
| `backend/tests/test_search_optimization.py` | 新建（4 个测试） |
| `backend/tests/test_teacher_students_pagination.py` | 新建（2 个测试） |
| `backend/tests/test_question_duplicate_candidates.py` | 新建（2 个测试） |
| `backend/tests/test_auth.py` | 修改（学生列表断言适配） |
| `backend/tests/test_integration_bugfixes.py` | 修改（学生空课程断言适配） |
| `backend/tests/test_public_question_contribution.py` | 修改（路由路径替换） |
| `backend/tests/test_student_course_visibility.py` | 修改（移除兼容路径循环） |
| `backend/tests/test_admin_public_material_validation.py` | 修改（路由路径替换） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮记录） |
| `docs/superpowers/project-map.md` | 修改（补充长期约定） |

### 验证情况

- 前端 `npm run build` 通过（含 vue-tsc type-check）。
- 后端 `py -m pytest tests/ -q`：193 passed, 3 errors（3 个 errors 来自 `test_deploy_env_check.py` 的 `PermissionError`，与本次改动无关）。
- 全部 10 个新增测试通过。

### 服务器部署影响

- 需要拉取代码并重启后端服务（大量路由、服务层和模型层改动）。
- 需要重新构建前端并部署静态资源。
- 不需要执行数据库迁移。
- 不需要修改环境变量或 Nginx 配置。

### 遗留问题

- 无

建议继续按下面格式追加：

```text
## 第 N 轮：标题

### 修改时间
...

### 修改目标
...

### 修改内容
...

### 验证情况
...

### 服务器部署影响
...

### 遗留问题
...
```

这样项目的演进历史会非常清晰。
## 第 30 轮：公共课程题库共享与负责记录

### 修改时间

2026-06-27

### 修改目标

实现所有教师共用一套公共题库：教师在公共课程副本的新增/导入题目，会回写到公共源课程并同步到所有教师副本；管理员可按公共课程查看题库负责记录。

### 修改内容

1. **新增题库贡献记录模型与兼容表**
   - 文件：`backend/app/models/entities.py`、`backend/app/db/schema_compat.py`
   - 新增 `QuestionContributionLog` 实体，按必要使用快照存储 `public_course_id/public_course_name/operator_id/operator_name/operator_role/action/question_count/created_at`
   - `schema_compat` 会在旧数据库中动态补建 `question_contribution_logs` 表及三条索引，旧库启动后可自动启用

2. **题库写入改为公共源回写**
   - 文件：`backend/app/services/question_service.py`、`backend/app/services/admin_public_course_service.py`、`backend/app/services/question_contribution_service.py`
   - 教师在公共课程副本中新增/导入题目时，先解析到源公共课程写回，再同步到所有教师副本，并在成功后续一条负责记录
   - 私有课程保留原有行为，不会写入负责记录

3. **管理员端联动**
   - 文件：`backend/app/api/v1/routes/admin_public_course_routes.py`、`frontend/src/api/adminPublicCourse.ts`、`frontend/src/views/admin/AdminPublicCourses.vue`
   - 新增 `GET /api/admin/public-courses/{course_id}/question-contributions`，前端在公共课程相应 Tab 中显示负责记录、分页与操作人详情

4. **测试补充**
   - 文件：`backend/tests/test_public_question_contribution.py`、`backend/tests/test_schema_compat.py`
   - 覆盖教师在公共课程副本新增、批量导入、管理员查询贡献记录，以及旧库补建表的兼容测试

### 验证情况

- `python -m pytest backend/tests/test_public_question_contribution.py backend/tests/test_schema_compat.py -q` → 20 passed
- `cd frontend && npm run build` → 失败，当前仓库中 `frontend/src/views/PracticeQuizView.vue` 和 `frontend/src/views/ProjectUploadView.vue` 存在独立 TypeScript 发错，与本次公共题库共享功能无关

### 服务器部署影响

- 需要拉取代码并重启后端服务，启动时会自动补建 `question_contribution_logs` 表及索引
- 需要重新构建前端，让新的负责记录与 Tab 生效
- 不需手动数据库迁移，也不需修改环境变量或 Nginx 配置

### 遗留问题

- 前端全量构建仍被仓库内既有类型错误阻塞，后续需要单独修复

## 第 31 轮：学生端首页学校标识与顶部图标更新

### 修改时间

2026-06-27

### 修改目标

按首页截图反馈，在学生端首页首屏补充“中国计量大学”学校归属，并替换顶部左侧旧的单字“学”圆章图标，让平台标识更贴合中国计量大学 AI 通识课程语境。

### 修改内容

1. **首页首屏补充学校归属**
   - 文件：`frontend/src/components/home/HeroSection.vue`
   - 将首屏徽标改为“中国计量大学 · AI 通识教育课程平台”
   - 新增“量”字圆形视觉标记，作为非官方校徽替代标识，保持与当前靛青、书卷风格一致

2. **顶部左侧图标更新**
   - 文件：`frontend/src/components/AppHeader.vue`
   - 将旧的单字“学”圆章替换为书本与圆规组合的内联 SVG 图标
   - 顶部品牌副标题补充为“中国计量大学 · AI 通识课平台”
   - 为新图标增加“中国计量大学 AI 通识课平台标识”中文可访问名称

3. **静态测试补充**
   - 文件：`frontend/tests/home-branding-static.test.mjs`
   - 覆盖首页首屏学校名、徽标视觉标记、顶部品牌副标题、新图标可访问名称，以及不再使用旧“学”字圆章

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `frontend/src/components/home/HeroSection.vue` | 修改（首页首屏学校标识） |
| `frontend/src/components/AppHeader.vue` | 修改（顶部图标与副标题） |
| `frontend/tests/home-branding-static.test.mjs` | 新增（首页品牌静态测试） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮记录） |

### 验证情况

- `cd frontend && node .\tests\home-branding-static.test.mjs`：通过
- `cd frontend && npm run build`：通过

### 服务器部署影响

- 需要服务器拉取代码并重新构建前端，让首页首屏与顶部图标更新生效
- 后端服务不需要重启
- 不需要数据库迁移
- 不需要修改环境变量或 Nginx 配置

### 遗留问题

- 本次未使用官方校徽图片；如果后续提供正式校徽素材，可在当前 SVG 标识基础上替换为官方资产

## 第 32 轮：课程资料与题库删除权限调整

### 修改时间

2026-06-27

### 修改目标

按最新权限边界收紧教师端删除能力：教师可以删除自己课程下的资料，公共课程同步到副本里的资料删除只作用于教师副本，不影响公共源；题目删除只允许管理员执行，教师仅保留新增和编辑自己课程题目的能力；界面补充“公共/私有”标签，方便老师识别来源。

### 修改内容

1. **教师端资料删除放开到课程副本级别**
   - 文件：`backend/app/services/material_service.py`、`frontend/src/views/teacher/TeacherMaterials.vue`、`frontend/src/views/teacher/TeacherCourseDetail.vue`
   - 教师删除资料时不再拦截同步资料，删除只影响自己的课程副本。
   - 前端删除确认文案补充“不会影响公共课程源内容”，资料列表和课程详情资料卡片补充“公共/私有”标签。

2. **教师端题目删除封死，编辑保留**
   - 文件：`backend/app/api/v1/routes/question_routes.py`、`backend/app/services/question_service.py`、`frontend/src/views/teacher/TeacherQuestions.vue`
   - 教师端删题和批量删题入口直接返回 403，提示联系管理员处理。
   - 教师题库列表仅保留“编辑”入口，不再把同步题渲染成只读。
   - 教师编辑自己课程中的同步题目时允许保存，公共源题目仍由管理员端负责删除。

3. **教师删除课程时仅清理自己的副本**
   - 文件：`backend/app/services/question_service.py`、`frontend/src/views/teacher/TeacherCourses.vue`
   - 教师删除课程会级联清理本课程关联数据，但不会影响公共源课程及其内容。
   - 课程列表补充来源标签，删课确认文案明确只删除自己的课程副本。

4. **回归测试补充**
   - 文件：`backend/tests/test_integration_bugfixes.py`
   - 更新教师删除/编辑同步题、删除同步资料、删除课程副本等回归用例。
   - 补充管理员删除公共题目的验证，确认源题和教师副本都能被同步清理。

### 验证情况

- `python -m pytest backend/tests/test_integration_bugfixes.py -q`：49 passed
- `cd frontend && npm run build`：通过
- `graphify update .`：两次执行均超时，最长等待 300 秒，未完成图谱同步

### 服务器部署影响

- 需要服务器拉取代码并重启后端服务，让题目删除权限和资料删除逻辑生效
- 需要服务器重新构建前端，让教师端删除入口、标签和确认文案生效
- 不需要数据库迁移
- 不需要修改环境变量或 Nginx 配置

### 遗留问题

- `graphify update .` 未能在本地完成，后续可在更长超时时间或排除异常测试文件后重跑

---

## 第 25 轮：全站共享题库与重复题拦截（2026-06-29）

### 背景

整个网站只服务"人工智能通识"这一门课，A/B/C 只是不同教学形式。题库不应再按课程私有化：任何老师在任意课程里新增/导入的题目，其他课程都应立即可见、可筛选；同时硬性拦截重复题。

设计稿位于 `docs/superpowers/specs/2026-06-28-ai-general-course-shared-question-bank-design.md`。

### 修改内容

1. **全站查重 / 列表 / 计数三件套**
   - 文件：`backend/app/services/question_bank_service.py`
   - 新增 `list_all_questions(db)` / `count_all_questions(db)`，`find_duplicate_question` 改为全站扫描（不再带 `course_id` 入参）。
   - 删除已废弃的 `count_questions_for_course`（旧"按根 id 过滤"语义）。

2. **教师端读写走全站**
   - 文件：`backend/app/services/question_service.py`
   - `list_questions` 移除 `course_id` 和 `teacher_id` 过滤，全站可见。
   - `create_question` / `import_questions_from_excel`：题目原地写入所选课程（`course_id` 保留为创建课程），查重走全站；公共课程才记贡献日志。
   - `update_question` 查重走全站；编辑权限维持现状（题所在课程的 `created_by` 即创建者）。
   - `get_course_questions(course_id)` 返回全站所有题目，供作业按 `ids` 再过滤。
   - `delete_course` 改为先把名下题目转挂到其他课程再删，避免误删全站共享题。
   - `get_course_detail` 的 `question_count` 改为全站题数。

3. **管理员端走全站 + 删课保护共享根**
   - 文件：`backend/app/services/admin_public_course_service.py`
   - `list_public_questions` / `create_public_question` / `update_public_question` / `delete_public_question` / `import_questions_to_public_course` 全部走全站查重和全站读取。
   - `update_public_question` 移除副本同步、`delete_public_question` 移除副本删除。
   - `delete_public_course` 增加共享根保护：若被删课程是其他课程的共享根（有别的课程指向它）则拒绝删除；删非根公共课时把课程名下的题转挂到另一门课，保住全站共享题。

4. **停止题目副本分发**
   - 文件：`backend/app/services/public_course_sync_service.py`
   - `mirror_public_course_content` 移除题目复制循环（资料和阶段仍按现有机制复制副本）。
   - 删除已无引用的 `copy_question_to_course` / `sync_question_to_course_copies` / `delete_synced_questions`。

5. **路由层 `is_owner` 标识**
   - 文件：`backend/app/api/v1/routes/question_routes.py`、`backend/app/api/v1/routes/admin_public_course_routes.py`
   - `_format_question` 增加 `is_owner` 字段，供前端判断当前教师能否编辑该题。
   - 管理员公共课列表 `_format_course` 的 `question_count` 改为全站题数。

6. **课程列表题数全站化**
   - 文件：`backend/app/services/course_response_service.py`
   - `_format_course` 的 `question_count` 改为 `db.query(Question).count()`，所有课程入口看到的题数都是全站题库。

7. **前端适配全站题库**
   - 文件：`frontend/src/api/question.ts`、`frontend/src/views/teacher/TeacherQuestions.vue`
   - `Question` 接口增加可选 `is_owner` 字段。
   - 题库管理页移除"全部课程"筛选下拉（全站共享后已无意义），过滤项简化为题型/关键词/标签。
   - "共 N 题"文案改为"全站题库 共 N 题"，更清晰表达共享语义。
   - 表格"编辑"按钮按 `is_owner === false` 禁用，hover 提示"该题由其他老师创建，请联系管理员修改"，避免点击后端 404 的体验断层。

8. **测试改写**
   - 文件：`backend/tests/test_public_question_contribution.py`、`backend/tests/test_integration_bugfixes.py`、`backend/tests/test_question_import_skip.py`
   - `test_public_question_contribution.py`：
     - `test_question_bank_is_shared_across_teacher_courses` 断言改为"包含共享题目"（兼容 seed 题）。
     - 旧的"教师在副本写回公共源 + 复制副本 + 写日志"测试两个改写为"管理员在公共课程新增/导入 → 题目原地保存 → 写日志 → 教师入口全站可见"。
     - 分页日志/快照保留两个测试改用管理员路径触发日志。
   - `test_integration_bugfixes.py`：
     - `test_add_public_course_creates_owned_copy_with_content` / `test_add_public_course_records_source_ids`：移除题目副本断言（资料副本断言保留）。
     - `test_admin_question_changes_sync_and_teacher_can_edit_synced_copy` 重命名为 `test_admin_question_changes_are_globally_visible_and_teacher_cannot_delete`：移除副本同步断言，验证管理员改题全站可见、教师不能编辑/删除非自己创建的题、管理员删题全站消失。
     - `test_admin_public_question_tags_are_normalized_and_synced_to_teacher_copy` 重命名为 `test_admin_public_question_tags_are_normalized`：保留标签规范化断言，移除副本镜像断言。
     - `test_admin_import_public_questions...`：移除副本镜像断言。
     - `test_delete_public_course_unlinks_teacher_copy_content` 重命名为 `test_delete_public_course_unlinks_teacher_copy_and_preserves_shared_questions`：验证副本资料解绑 + 公共课名下的题转挂到其他课程保留在全站。
   - `test_question_import_skip.py`：
     - `test_same_stem_different_course_not_skipped` 重命名为 `test_same_stem_different_course_is_skipped_in_shared_bank`，断言改为"不同课程下相同题干被全站查重拦截并跳过"。

### 验证情况

- `cd backend && py -m pytest tests/ -q --ignore=tests/test_deploy_env_check.py`：**161 passed**
  - 基线 9 failed → 0 failed
  - `test_deploy_env_check.py` 的 3 个 ERROR 为本机临时目录权限问题（`PermissionError: pytest-of-ASUS`），与本任务无关
- `cd frontend && npm run build`：通过（含 type-check）

### 服务器部署影响

- **需要拉取代码**并重启后端服务，让全站共享题库语义生效
- **需要重新构建前端**（`cd frontend && npm run build`），让题库管理页文案与编辑按钮禁用生效
- **不需要手动 SQL 迁移**：题目原地保存、零归并，旧数据零迁移可读；`question_bank_root_course_id` 列由 `schema_compat.py` 在启动时自动补齐
- **不需要修改环境变量**

### 风险与注意事项

- 上线后任意课程的题目修改会影响全站，这是预期行为（全站共享共建）
- 共享根公共课程受删除保护，不可随意删除；非根公共课删除时其题目会转挂到其他课程
- 教师只能编辑自己创建（即题所在课程的 `created_by`）的题，删除统一禁止；管理员可改全部
---

## 第 33 轮：共享题库作业链路与自由练习统计修复（2026-06-29）

### 修改目标

修复全站共享题库上线后的作业链路回归：教师端可以在题库选择器中看到全站共享题，但发布作业、学生取题、答题提交仍按旧逻辑要求题目属于当前课程，导致“看得到、选得上、发不出/答不了”。同时统一学生自由练习统计口径，使课程入口展示的全站题库数量与统计结果一致。

### 修改内容

1. **发布作业校验适配全站题库**
   - 文件：`backend/app/services/announcement_service.py`
   - `create_announcement` 校验题目时不再要求 `Question.course_id == course_id`，改为校验 `question_ids` 全部存在于全站题库。
   - 错误提示同步改为“题目必须来自全站题库”。

2. **作业取题与提交适配共享题**
   - 文件：`backend/app/services/task_service.py`、`backend/app/services/quiz_service.py`
   - `get_assignment_questions` 按公告 `question_ids` 全站取题，并继续按公告保存顺序返回。
   - `submit_answer` 在作业模式下只校验题目是否属于该公告 `question_ids`，不再校验题目原始 `course_id`。
   - 课程、班级、作业访问权限仍由 `get_accessible_assignment` 和 `student_can_access_course` 保持约束。

3. **自由练习提交与课程统计口径统一**
   - 文件：`backend/app/services/quiz_service.py`
   - 学生自由练习提交共享题时，只要求学生至少加入了一个有关联课程的班级，避免全站题因原始 `course_id` 不属于该学生而被拒绝。
   - `get_course_quiz_stats` 改为统计该学生的自由练习记录（`announcement_id is None`）在全站题库中的作答数和正确率，与课程卡片“全站题库题数”一致。

4. **回归测试补充**
   - 文件：`backend/tests/test_assignment_practice_flow.py`
   - 新增教师用其他课程归属的共享题发布作业、学生获取并提交共享题作业的测试。
   - 新增学生自由练习提交共享题并纳入课程统计的测试。

5. **实施计划记录**
   - 文件：`docs/superpowers/plans/2026-06-29-shared-question-assignment-fix.md`
   - 记录问题范围、实施步骤、验收方式和服务器部署影响。

### 验证情况

- `python -m pytest backend/tests/test_assignment_practice_flow.py -q`：9 passed
- `python -m pytest backend/tests/test_assignment_practice_flow.py backend/tests/test_public_question_contribution.py backend/tests/test_question_import_skip.py -q`：22 passed
- `cd frontend && npm run type-check`：通过
- `cd frontend && npm run build-only`：通过，仅保留 Vite chunk 体积提示
- 扩大运行 `python -m pytest backend/tests/test_assignment_practice_flow.py backend/tests/test_public_question_contribution.py backend/tests/test_question_import_skip.py backend/tests/test_integration_bugfixes.py -q` 时，70 passed，1 failed；失败为 Windows 下 `test_batch_download_only_uses_teacher_students` 在 finally 中删除 PDF 测试文件时触发 `PermissionError`，与本轮共享题库作业链路无关。

### 服务器部署影响

- 需要服务器拉取代码并重启后端服务，让作业发布、作业取题、答题提交和课程练习统计的新语义生效。
- 前端业务代码本轮未修改，原则上不需要重新构建前端；如果部署流程固定前后端一起发布，可以顺带重新构建。
- 不需要数据库迁移。
- 不需要修改环境变量或 Nginx 配置。

### 遗留问题

- Windows 本地批量下载测试仍可能因测试文件句柄释放时机出现 `PermissionError`，建议后续单独修复测试清理逻辑或流关闭逻辑。

---

## 第 34 轮：课程大文件预览加速试点（2026-06-29）

### 改动概述

落地课程大文件预览加速试点，让 1GB 级 PDF / 视频经后端鉴权后由 Nginx 传输，并在学生端、教师端展示图文资料卡片与站内预览。

### 改动明细

1. **后端模型与 Schema**
   - `backend/app/models/entities.py`：新增 `MaterialPreview` ORM，`Material` 增加 `preview` relationship。
   - `backend/app/schemas/common.py`：新增 `MaterialPreviewOut`，`MaterialOut` 增加 `preview` 字段。
   - `backend/app/db/schema_compat.py`：兼容创建 `material_previews` 表和索引（SQLite + MySQL 双方言）。

2. **文件鉴权与 Nginx 内部跳转**
   - `backend/app/services/file_service.py`：新增 `normalize_local_object_key` 路径规范化和 `build_x_accel_redirect_path` 内部路径构造。
   - `backend/app/services/material_service.py`：新增 `resolve_material_file_for_user` 权限校验和文件定位、`format_material_preview` 预览格式化。
   - `backend/app/api/v1/routes/material_routes.py`：新增 `GET /materials/{material_id}/file`（鉴权 + X-Accel-Redirect）和 `POST /materials/{material_id}/preview/rebuild`（教师重建预览）。
   - `deploy/nginx.conf`：新增 `/_protected_uploads/` internal 目录，启用 sendfile + aio threads。

3. **预览生成服务**
   - 新建 `backend/app/services/material_preview_service.py`：`ensure_material_preview`、`mark_material_preview_failed`、`generate_material_preview`，PDF 分支使用 PyMuPDF 提取封面和摘要，视频分支使用 ffprobe + ffmpeg 提取元数据和封面帧。
   - `backend/requirements.txt`：新增 `PyMuPDF` 依赖。
   - `backend/app/services/course_response_service.py`：`_format_material` 增加 `preview` 字段返回。
   - 资料创建后自动调用 `ensure_material_preview` 建立 pending 记录。

4. **前端 API 与通用组件**
   - `frontend/src/api/material.ts`：新增 `MaterialPreview` 类型、`preview` 字段、`getMaterialFileUrl()`、`rebuildMaterialPreview()`。
   - 新建 `frontend/src/components/common/MaterialPreviewDialog.vue`：统一 PDF / 视频站内预览弹窗。
   - 新建 `frontend/src/components/common/MaterialRichCard.vue`：图文资料卡片（封面、摘要、元数据、状态标签、操作按钮）。

5. **页面接入**
   - `frontend/src/views/CourseDetailView.vue`：学生课程详情使用 `MaterialRichCard` + `MaterialPreviewDialog`。
   - `frontend/src/views/teacher/TeacherMaterials.vue`：教师资料管理从表格改为图文卡片列表，支持预览和重建。
   - `frontend/src/views/teacher/TeacherCourseDetail.vue`：教师课程详情阶段资料和未分类资料均使用图文卡片。

6. **测试**
   - 新建 `backend/tests/test_material_file_acceleration.py`：6 个测试（权限、X-Accel-Redirect、路径穿越、课程内容预览字段）。
   - 新建 `backend/tests/test_material_preview_service.py`：3 个测试（pending 创建、失败记录、缺文件拒绝）。
   - 修改 `backend/tests/test_schema_compat.py`：新增 `material_previews` 表断言。
   - 新建 `frontend/tests/material-preview-static.test.mjs`：16 条断言覆盖 API、组件和三个页面。
   - 修改 `frontend/tests/local-file-preview-static.test.mjs`：适配新预览弹窗和资料专用接口。
   - 修改 `frontend/tests/nginx-local-file-preview-static.test.mjs`：新增 `_protected_uploads` 配置断言。

7. **文档同步**
   - `docs/superpowers/project-map.md`：新增长期约定（预览表、资料专用接口、图文卡片统一展示）和后端文件列表（预览服务）。
   - `docs/superpowers/frontend-guidelines.md`：新增教师端图文卡片和站内预览验收要求。
   - `docs/superpowers/plans/2026-06-28-large-material-preview-pilot.md`：状态更新为"第一版已实施，待服务器压测"。

### 验证情况

- `py -m pytest backend/tests/ -q`：172 passed，3 errors（均为 Windows 临时目录权限问题，与本轮无关）。
- `node frontend/tests/local-file-preview-static.test.mjs`：通过。
- `node frontend/tests/material-preview-static.test.mjs`：通过。
- `node frontend/tests/nginx-local-file-preview-static.test.mjs`：通过。
- `cd frontend && npm run build`：vue-tsc type-check + vite build 均通过。

### 服务器部署影响

- **需要拉代码**：后端和前端均有改动。
- **需要数据库兼容建表**：启动后端时 `ensure_schema_compatibility` 自动创建 `material_previews` 表和索引，无需手动执行 SQL。
- **需要重启后端**：新增路由、服务和模型生效。
- **需要重新构建前端**：`npm run build`，部署新 dist。
- **需要 Nginx reload**：新增 `/_protected_uploads/` internal 目录配置，alias 指向实际 `LOCAL_UPLOAD_DIR`。
- **需要确认服务器依赖**：`ffmpeg`、`ffprobe`（视频预览）、`PyMuPDF`（PDF 预览，随 requirements.txt 安装）。`qpdf` 可选。
- **环境变量**：确认 `.env` 中 `ALLOW_QUERY_TOKEN_FOR_FILES=true`，否则 `<video>` / `<object>` / 新窗口链接无法携带 Authorization。
- **部署后验收**：使用 1GB PDF 和 1GB 视频做基线对比，确认 `X-Accel-Redirect` 响应头和 Range 请求正常，后端 API 不被大文件传输阻塞。

---

## 第 35 轮：课程大文件预览试点影响复查与遗留 Bug 修复（2026-06-29）

### 复查范围

- 复查计划：`docs/superpowers/plans/2026-06-28-large-material-preview-implementation.md`。
- 复查代码影响面：资料专用文件接口 `/api/materials/{material_id}/file`、统一文件 URL 工具、`MaterialPreviewDialog` 的 PDF / 视频站内预览、资料预览相关后端测试和前端静态检查。

### 发现并修复的问题

1. **站内预览无法用 URL token 访问资料专用文件接口**
   - 根因：`MaterialPreviewDialog` 使用 `<video>` / `<object>` 直接请求 `/api/materials/{id}/file`，浏览器元素不会携带 axios 的 `Authorization` 头；前端 `resolveFileUrl()` 只给 `/api/files/{id}` 追加 `?token=`，后端 `get_current_user()` 的 query token 回退也只允许 `/api/files/`。
   - 影响：`ALLOW_QUERY_TOKEN_FOR_FILES=true` 时，旧统一文件入口可在新窗口、iframe、video 等场景访问，但新增资料专用预览接口仍会返回认证失败，导致 PDF / 视频站内预览打不开。
   - 修复：`frontend/src/utils/url.ts` 对 `/api/materials/` 文件路径追加 URL token；`backend/app/core/security.py` 在 `ALLOW_QUERY_TOKEN_FOR_FILES=true` 时额外允许 `/api/materials/{id}/file` 使用 query token，并限定到 `*/file`，不放开其他资料管理接口。
   - 回归测试：`backend/tests/test_material_file_acceleration.py` 新增 `test_material_file_allows_query_token_for_browser_preview`；`frontend/tests/local-file-preview-static.test.mjs` 新增资料专用文件入口 token 断言。

### 验证情况

- `py -m pytest backend\tests\test_material_file_acceleration.py::test_material_file_allows_query_token_for_browser_preview -q`：先失败，修复后通过。
- `node frontend\tests\local-file-preview-static.test.mjs`：先失败，修复后通过。
- `py -m pytest backend\tests\test_material_file_acceleration.py backend\tests\test_material_preview_service.py backend\tests\test_schema_compat.py -q`：26 passed。
- `node frontend\tests\local-file-preview-static.test.mjs`：通过。
- `node frontend\tests\material-preview-static.test.mjs`：通过。
- `node frontend\tests\nginx-local-file-preview-static.test.mjs`：通过。
- `cd frontend && npm run build`：`vue-tsc` type-check 和 `vite build` 均通过；仍有既有 chunk 体积提示。

### 服务器部署影响

- **需要拉代码并重启后端**：`backend/app/core/security.py` 的 query token 放行规则需要后端重启后生效。
- **需要重新构建并部署前端**：`frontend/src/utils/url.ts` 会影响生产构建产物，需重新执行前端构建并发布 dist。
- **不需要新增数据库迁移**：本轮只修复鉴权回退和前端 URL 拼接，不改变数据库结构；第 34 轮的 `material_previews` 建表要求仍然保留。
- **不需要修改 Nginx 配置**：沿用第 34 轮新增的 `/_protected_uploads/` internal 配置。
- **需要确认环境变量**：服务器仍需设置 `ALLOW_QUERY_TOKEN_FOR_FILES=true`，否则浏览器原生 `<video>` / `<object>` / 新窗口打开无法携带 Authorization。

---

## 第 36 轮：学习页面图文一体改造 — Task 1 后端模型与 Schema

### 修改时间

2026-06-29

### 修改目标

为学习页面图文一体改造建立数据层基础：新增 `lessons` 课时表与 `course_progress` 学习进度表，以及对应 Pydantic Schema。

### 修改内容

1. **新增 `Lesson` 模型**
   - 文件：`backend/app/models/entities.py`
   - 新增 `lessons` 表，字段：id、course_id、title、sort_order、content、status、created_at、updated_at
   - `status` 使用 `Enum("draft", "published")`，默认 `published`
   - `Course` 模型补充 `lessons` relationship，按 `sort_order` 排序并级联删除

2. **新增 `CourseProgress` 模型**
   - 文件：`backend/app/models/entities.py`
   - 新增 `course_progress` 表，字段：id、user_id、course_id、last_lesson_id、updated_at
   - 添加 `(user_id, course_id)` 联合唯一索引

3. **新增课时与学习进度 Schema**
   - 文件：`backend/app/schemas/common.py`
   - 新增 `LessonCreate`、`LessonUpdate`、`LessonOut`、`LessonReorderItem`、`CourseProgressIn`、`CourseProgressOut`

4. **项目地图更新**
   - 文件：`docs/superpowers/project-map.md`
   - 补充 `Course -> Lesson` 与 `CourseProgress` 数据关系
   - 补充课时与进度长期约定

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/models/entities.py` | 修改（新增 `Lesson`、`CourseProgress` 模型，补充 `Course.lessons`） |
| `backend/app/schemas/common.py` | 修改（新增课时与进度 Schema） |
| `docs/superpowers/project-map.md` | 修改（数据关系与约定更新） |

### 验证情况

- `py -m py_compile backend/app/models/entities.py backend/app/schemas/common.py` 通过
- 未新增路由、服务、测试；未修改 `.env`

### 服务器部署影响

- 本次仅新增模型与 Schema，不修改现有表数据
- 暂不需要服务器操作；待 Task 2/3 完成并合并后，统一执行数据库迁移（新增 `lessons`、`course_progress` 表）、重启后端、重新构建前端

### 遗留问题

- 需等待 Task 2/3 完成后执行数据库迁移并验证接口

---

## 第 37 轮：学习页面图文一体改造 — Task 3 后端学习进度接口

### 修改时间

2026-06-29

### 修改目标

为学习页面图文一体改造实现后端学习进度读写接口：`GET /api/courses/{course_id}/progress` 与 `POST /api/courses/{course_id}/progress`，供学生/教师/管理员记录和恢复最近学习的课时。

### 修改内容

1. **新增学习进度服务**
   - 文件：`backend/app/services/progress_service.py`（新建）
   - `get_progress(db, course_id, current_user)`：按 `(user_id, course_id)` 查询，返回 `last_lesson_id`；无记录返回 `None`
   - `save_progress(db, course_id, data, current_user)`：校验 `lesson_id` 必须属于该课程；存在记录则更新，不存在则新建；返回保存后的 `last_lesson_id`
   - 所有角色均使用 `current_user.id` 写入，确保学生只能写自己的进度

2. **新增学习进度路由**
   - 文件：`backend/app/api/v1/routes/progress.py`（新建）
   - `GET /courses/{course_id}/progress` → 返回 `{last_lesson_id: int | None}`
   - `POST /courses/{course_id}/progress` → 请求 `CourseProgressIn`，返回 `{last_lesson_id: int}`
   - 使用 `require_roles("student", "teacher", "admin")` 要求登录用户

3. **注册进度路由**
   - 文件：`backend/app/api/v1/__init__.py`
   - 导入 `progress_router` 并在 `lessons_router` 之后注册，未破坏已有路由

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/services/progress_service.py` | 新建（学习进度业务逻辑） |
| `backend/app/api/v1/routes/progress.py` | 新建（学习进度路由） |
| `backend/app/api/v1/__init__.py` | 修改（注册 `progress_router`） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮记录） |

### 验证情况

- `py -m py_compile backend/app/services/progress_service.py backend/app/api/v1/routes/progress.py backend/app/api/v1/__init__.py` 通过
- `py -m pytest backend/tests/ -q`：174 passed，3 errors（均为 `test_deploy_env_check.py` 的 Windows 临时目录权限问题，与本轮改动无关）
- 未修改模型、Schema、`.env` 或 Task 2 代码

### 服务器部署影响

- **需要拉代码**：后端新增服务与路由文件
- **需要重启后端**：`progress_router` 注册和新服务生效
- **需要数据库建表**：`course_progress` 表已在 Task 1 模型中定义，首次部署 Task 3 前需确保执行过数据库迁移（或依赖 `ensure_schema_compatibility` 自动建表）
- **不需要重新构建前端**：本轮仅后端接口
- **不需要修改 Nginx 配置或环境变量**

---

## 第 38 轮：学习页面图文一体改造 — Task 2/4/5/6/7/8/9 完成与验证

### 修改时间

2026-06-29

### 修改目标

完成学习页面图文一体改造剩余全部 Task：后端课时管理接口、前端 wangEditor 编辑器、前端课时 API、教师端课时管理、学生端书籍式阅读器、学生端课程列表"继续学习"、以及全流程测试验证。

### 修改内容

1. **后端课时管理接口（Task 2）**
   - 文件：`backend/app/services/lesson_service.py`（新建）、`backend/app/api/v1/routes/lessons.py`（新建）
   - 实现课时 CRUD、排序、按课程查询；校验课时归属课程；教师/管理员可操作
   - 在 `backend/app/api/v1/__init__.py` 注册 `lessons_router`

2. **前端 wangEditor 课时编辑器（Task 4）**
   - 文件：`frontend/src/components/lesson/LessonEditor.vue`（新建）
   - 基于 `@wangeditor/editor` 实现富文本编辑
   - 支持插入已有视频/PDF 资料占位符（`.lesson-material` + `data-material-id/type`）

3. **前端课时与学习进度 API（Task 5）**
   - 文件：`frontend/src/api/lesson.ts`（新建）、`frontend/src/api/progress.ts`（新建）
   - 封装 `getLessons`、`createLesson`、`updateLesson`、`deleteLesson`、`reorderLessons` 等接口
   - 封装 `getCourseProgress`、`saveCourseProgress` 接口

4. **教师端课程详情页课时管理（Task 6）**
   - 文件：`frontend/src/views/teacher/TeacherCourseDetail.vue`
   - 新增"课时"标签页，支持新增、编辑、删除、排序课时
   - 集成 `LessonEditor.vue` 编辑课时内容

5. **学生端书籍式阅读器（Task 7）**
   - 文件：`frontend/src/components/lesson/CourseToc.vue`（新建）
   - 文件：`frontend/src/components/lesson/LessonReader.vue`（新建）
   - 文件：`frontend/src/components/lesson/PrevNextNav.vue`（新建）
   - 文件：`frontend/src/views/CourseDetailView.vue`（重构）
   - 实现左侧目录树、右侧课时内容渲染、底部前后导航
   - `LessonReader` 将 `.lesson-material` 占位符替换为 `<video>` 或 PDF 预览卡片
   - 支持 URL `lesson_id` 查询参数、学习进度恢复、移动端抽屉式目录
   - 保留原有"资料"标签页入口

6. **学生端课程列表页优化（Task 8）**
   - 文件：`frontend/src/views/LearnView.vue`
   - 加载课程列表后，并行获取每门课的学习进度和课时列表
   - 课程卡片显示"继续学习"或"开始学习"按钮，以及进度百分比条
   - "继续学习"跳转至 `/learn/course/{id}?lesson_id={lastLessonId}`

7. **测试与验证（Task 9）**
   - `npm run type-check` 通过
   - `npm run build` 通过
   - `py -m pytest tests/test_integration_bugfixes.py -v`：49 passed
   - `py -m pytest tests/ -q`：174 passed，3 errors（`test_deploy_env_check.py` 的 Windows 临时目录权限问题，与本次改动无关）

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/services/lesson_service.py` | 新建 |
| `backend/app/api/v1/routes/lessons.py` | 新建 |
| `backend/app/api/v1/__init__.py` | 修改（注册 `lessons_router`） |
| `frontend/src/components/lesson/LessonEditor.vue` | 新建 |
| `frontend/src/components/lesson/CourseToc.vue` | 新建 |
| `frontend/src/components/lesson/LessonReader.vue` | 新建 |
| `frontend/src/components/lesson/PrevNextNav.vue` | 新建 |
| `frontend/src/api/lesson.ts` | 新建 |
| `frontend/src/api/progress.ts` | 新建 |
| `frontend/src/views/CourseDetailView.vue` | 重构 |
| `frontend/src/views/teacher/TeacherCourseDetail.vue` | 修改（新增课时管理标签页） |
| `frontend/src/views/LearnView.vue` | 修改（继续学习、进度展示） |
| `.trae/specs/learn-page-doc-style/tasks.md` | 修改（任务状态同步） |
| `.trae/specs/learn-page-doc-style/checklist.md` | 修改（验收项同步） |

### 验证情况

- 前端 `npm run type-check` 无新增类型错误
- 前端 `npm run build` 构建成功
- 后端 `tests/test_integration_bugfixes.py` 49 项全部通过
- 后端全量测试 174 passed，仅 3 个 `test_deploy_env_check.py` 因 Windows 临时目录权限报错，与本次改动无关

### 服务器部署影响

- **需要拉取代码**：新增多个后端服务/路由文件和前端组件
- **需要重启后端服务**：`lessons_router` 和 `progress_router` 注册生效
- **需要重新构建前端**：LearnView.vue、CourseDetailView.vue 及多个新组件均参与构建
- **需要数据库建表**：确保 `lessons` 表和 `course_progress` 表已创建（可依赖 `ensure_schema_compatibility` 自动补建，或手动执行迁移）
- **不需要修改 Nginx 配置或环境变量**
- **部署后验收**：
  - 教师端进入课程详情"课时"标签页，可创建、编辑、删除、排序课时
  - 学生端进入课程列表，有进度的课程显示"继续学习"和进度条
  - 学生端进入课程详情，左侧目录、右侧内容、底部导航正常，资料占位符正确渲染

### 遗留问题

- 无

---

## 第 39 轮：学习页面图文一体改造审查补强

### 修改时间

2026-06-29

### 修改目标

复查 Trae 已完成的“学”页面图文一体改造，补齐学生端实际可用性、权限边界、富文本安全、数据库兼容迁移和旧代码残留问题。

### 修复内容

1. **修复学生端无法读取课时**
   - 文件：`backend/app/api/v1/routes/lessons.py`、`backend/app/services/lesson_service.py`
   - GET 课时列表和课时详情改为所有登录用户可进入服务层校验。
   - 学生只能读取自己已加入课程下的 `published` 课时；草稿和未加入课程统一按不可见处理。
   - 教师只能读取/管理自己创建的课程课时，管理员可管理全部。

2. **补齐学习进度访问边界**
   - 文件：`backend/app/services/progress_service.py`
   - 读取和保存进度前先校验课程访问权限。
   - 学生不能为未加入课程或草稿课时保存进度。

3. **修复富文本 XSS 风险**
   - 新增：`backend/app/core/html_sanitizer.py`
   - 创建、更新和读取课时内容时均进行 HTML 白名单清洗。
   - 保留常用排版标签、图片链接和 `.lesson-material` 资料占位符，移除 `script`、`iframe`、事件属性、`javascript:` 链接等危险内容。

4. **修复删除课时契约和悬挂进度引用**
   - 文件：`backend/app/api/v1/routes/lessons.py`、`backend/app/services/lesson_service.py`、`backend/app/models/entities.py`
   - 删除接口返回 `{id}`，与前端 `deleteLesson()` 类型一致。
   - 删除课时前清空 `course_progress.last_lesson_id` 引用，避免历史进度指向已删除课时。
   - ORM 外键补齐 `ondelete` 语义。

5. **补齐数据库兼容和迁移路径**
   - 文件：`backend/app/db/schema_compat.py`
   - 修复 MySQL `course_progress` 建表唯一键语法，改为 `UNIQUE KEY uq_course_progress_user_course (user_id, course_id)`。
   - 新增：`backend/migrations/versions/20260629_add_lessons_course_progress.py`，补齐正式 Alembic 迁移文件。

6. **清理前端重复入口和开发态隐患**
   - 文件：`frontend/src/views/LearnView.vue`、`frontend/src/views/CourseDetailView.vue`
   - 课程卡片去掉重复“资料 + 开始/继续学习”入口，收敛为一个主按钮：继续学习、开始学习或查看资料。
   - 课程详情支持 `?tab=materials`，无课时时自动切到资料页。
   - 文件：`frontend/src/components/lesson/LessonEditor.vue`
   - wangEditor 自定义菜单注册改用全局标记，降低 HMR 重复注册风险。
   - 文件：`frontend/src/components/lesson/LessonReader.vue`
   - 资料占位符查找改为 Map，避免模板中重复遍历资料列表。

### 新增/更新测试

- 新增并完善：`backend/tests/test_lessons_progress.py`
  - 学生读取已发布课时。
  - 学生不能读取草稿或未加入课程课时。
  - 进度保存必须有课程访问权限且课时已发布。
  - 教师/管理员课时管理权限边界。
  - 删除课时返回 `{id}` 并清空旧进度引用。
  - 富文本创建、更新、读取均清洗危险内容。
- 修改：`backend/tests/test_schema_compat.py`
  - 增加 MySQL `course_progress` 唯一键语法回归测试。

### 验证情况

- `python -m pytest backend\tests\test_lessons_progress.py backend\tests\test_schema_compat.py -q`：通过。
- `python -m py_compile backend\migrations\versions\20260629_add_lessons_course_progress.py backend\app\core\html_sanitizer.py backend\app\services\lesson_service.py backend\app\services\progress_service.py backend\app\api\v1\routes\lessons.py`：通过。
- `cd frontend && npm run type-check`：通过。
- `cd frontend && npm run build`：通过，仍保留既有 chunk 体积提示。

### 服务器部署影响

- **需要拉取代码并重启后端**：课时路由、课时服务、进度服务、HTML 清洗器和模型外键语义有改动。
- **需要重新构建并部署前端**：学习页入口、课程详情页 tab、课时编辑器和阅读器组件有改动。
- **需要数据库建表/迁移确认**：新增正式 Alembic 迁移文件；已有部署也可继续依赖 `ensure_schema_compatibility` 自动补建 `lessons` 和 `course_progress` 表。首次上线前需确认二者至少执行一种。
- **不需要修改 Nginx 配置**：本轮未改变静态文件或大文件代理路径。
- **不需要调整环境变量**：本轮未新增环境变量。

---

## 第 40 轮：教师课程搜索与 Graphify 图谱残留清理

### 修改时间

2026-06-30

### 修改目标

补齐教师课程管理页“已添加课程”的关键词搜索，并清理 Graphify 图谱中已经从源码删除的 `ActivityEvent` 旧代码节点残留。

### 修改内容

1. **教师课程页已添加课程搜索**
   - 文件：`frontend/src/views/teacher/TeacherCourses.vue`
   - 新增 `mySearchKeyword` 状态和“搜索已添加课程”输入框。
   - 我的课程分页请求携带 `keyword` 和 `scope: 'owned'`，回车或清空搜索时回到第一页重新加载。

2. **补充静态回归测试**
   - 新增：`frontend/tests/teacher-courses-search-static.test.mjs`
   - 检查搜索状态、请求参数、搜索触发逻辑和输入框交互，避免后续把“我的课程搜索”遗漏。

3. **Graphify 图谱残留清理**
   - 更新：`graphify-out/graph.json`、`graphify-out/graph.html`、`graphify-out/GRAPH_REPORT.md`
   - 重新按当前 `backend/app/models/entities.py` AST 替换图谱中该源文件节点，移除旧 `models_entities_activityevent` 代码节点及相关旧边。

### 验证情况

- `cd frontend && node ./tests/teacher-courses-search-static.test.mjs`：通过。
- `cd frontend && npm run build`：通过，仍保留既有 chunk 体积提示。
- `rg -n -F "models_entities_activityevent" graphify-out`：无匹配。
- `rg -n -F '"label": "ActivityEvent"' graphify-out\graph.json graphify-out\graph.html graphify-out\GRAPH_REPORT.md`：无匹配。

### 服务器部署影响

- **需要拉取代码并重新构建、部署前端静态资源**：教师课程页新增已添加课程搜索输入框和请求参数。
- **不需要重启后端服务**：本轮未修改后端运行代码。
- **不需要数据库迁移**：本轮未调整数据库结构。
- **不需要修改 Nginx 配置或环境变量**。
- **Graphify 图谱更新只影响本地项目知识图谱**，不影响服务器运行。

### 2026-07-02 公开学习馆 P0 首页与游客访问

#### 修改内容

1. **后端公开学习只读接口**
   - 新增：`backend/app/services/public_learning_service.py` — 公开学习只读查询、公开课程格式化、公开资料文件权限校验。
   - 新增：`backend/app/api/v1/routes/public_learning_routes.py` — `/api/public/learning/*` 路由（公开课程列表/详情/课时/资料/资料文件预览）。
   - 修改：`backend/app/api/v1/__init__.py` — 注册公开学习路由。

2. **前端公开学习能力**
   - 新增：`frontend/src/api/publicLearning.ts` — 公开课程、课时、资料、资料文件 URL 接口封装。
   - 修改：`frontend/src/api/http.ts` — Axios 配置增加 `silentError` 标记，用于公开接口与鉴权接口之间的无噪声兜底。
   - 修改：`frontend/src/api/course.ts` — `getCourseDetail` 支持静默错误。
   - 修改：`frontend/src/components/common/MaterialPreviewDialog.vue` — 支持外部传入公开资料预览 URL。

3. **路由与页面改造**
   - 修改：`frontend/src/router/index.ts` — `/learn` 和 `/learn/course/:courseId` 标记为公开路由，守卫顺序调整为教师拦截早于 public 放行。
   - 修改：`frontend/src/views/LearnView.vue` — 游客读取公开课程和最新资料，登录学生额外合并已加入课程并显示进度；新增"公开学习"标记和资料预览区。
   - 修改：`frontend/src/views/CourseDetailView.vue` — 游客使用公开接口加载课程详情、课时和资料，登录学生优先鉴权接口失败后兜底公开接口；游客不保存学习进度。
   - 修改：`frontend/src/components/home/HeroSection.vue` — 首屏展示"中国计量大学 · 深度 AI 通识学习平台"，主按钮指向公开学习馆，副按钮登录保存进度。

4. **测试与回归保护**
   - 新增：`backend/tests/test_public_learning.py` — 公开课程隔离、草稿课时过滤、公开资料文件预览。
   - 新增：`frontend/tests/public-learning-static.test.mjs` — 静态检查公开路由、教师拦截顺序、公开 API、Nginx 配置。

#### 验证情况

- `backend\.venv\Scripts\pytest.exe backend\tests\test_public_learning.py backend\tests\test_lessons_progress.py backend\tests\test_material_file_acceleration.py -q`：19 passed。
- `node frontend\tests\public-learning-static.test.mjs`：所有断言通过。
- `cd frontend && cmd /c npm run type-check`：通过。
- `cd frontend && cmd /c npm run build`：通过（保留既有 Vite chunk size warning）。

#### 服务器部署影响

- **需要服务器拉取代码**：后端新增公开学习接口和前端页面改造。
- **需要重启后端服务**：新增公开学习路由需生效。
- **需要重新构建并部署前端静态资源**：路由、学习馆、课程详情页和首页均有改动。
- **不需要数据库迁移**：`Course.is_public` 字段已存在，由管理员后台控制。
- **不需要修改环境变量或 Nginx 配置**：公开资料预览依赖既有 `/_protected_uploads/` internal 配置，部署前确认该配置仍存在并指向实际上传目录即可。

---

## 第 41 轮：P1 书架卡片动效 — 课程卡片书本展开视觉升级

### 修改时间

2026-07-02

### 修改目标

将公开学习馆 `/learn` 页面的课程卡片从平面信息卡升级为具有"书本翻开"3D 动效的书架式卡片，提升公开学习页面的视觉吸引力。同时为资料条卡片增加书脊色带和书架感。

### 修改内容

1. **课程卡片 DOM 重构为书本双层结构**
   - 文件：`frontend/src/views/LearnView.vue`
   - 原平面 `.course-card` 改为 `.book-wrapper > .book-cover + .book-inner` 双层结构
   - 封面层（`.book-cover`）：左侧书脊色带（6px 渐变）、课程名、课时/资料计数、公开学习标签、进度条（登录学生）、行动按钮
   - 内页层（`.book-inner`）：课程简介（截断 4 行）、行动提示
   - 卡片外层添加 `role="button"`、`tabindex="0"`、`@keydown.enter` 保持键盘可访问
   - 按钮使用 `@click.stop` 避免事件冒泡导致双重跳转

2. **3D 书本翻开动效**
   - `.course-card` 设置 `perspective: 800px` 作为 3D 空间容器
   - `.book-wrapper` 设置 `transform-style: preserve-3d`、`transform-origin: left center`
   - hover 时 `rotateY(-18deg)` 沿左侧翻开，露出内页课程简介
   - 过渡动画 `400ms cubic-bezier(0.4, 0, 0.2, 1)` 柔和缓出
   - hover 时封面阴影从 `2px 2px 8px` 加深到 `8px 4px 20px`，模拟翻开投射

3. **资料条书架感增强**
   - `.material-mini-card` 添加 `::before` 伪元素作为 4px 书脊色带
   - PDF 资料使用紫色（`#6b4c8a`），视频资料使用学习主题色（`var(--color-learn)`）
   - 基础阴影 `2px 2px 8px`，hover 时 `translateX(2px)` + 阴影加深
   - 通过 `data-type` 属性区分资料类型

4. **响应式降级**
   - `> 900px`：2 列书架，完整 3D 翻转
   - `768-900px`：1 列书架，保留 3D
   - `< 768px`：1 列平面卡片，取消 3D（`perspective: none`、`transform: none`），内页层隐藏

5. **无障碍适配**
   - `prefers-reduced-motion: reduce` 媒体查询下禁用所有 3D 翻转和过渡动画
   - `focus-visible` 状态显示 `box-shadow` 聚焦环
   - 键盘可通过 Tab 聚焦、Enter 触发导航

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `frontend/src/views/LearnView.vue` | 修改（卡片 DOM 重构 + 书本 CSS + 资料条增强 + 响应式 + 无障碍） |

### 验证情况

- `npm run build` 通过（含 vue-tsc type-check），构建产物正常输出到 dist/
- 不需要后端改动，不涉及后端测试

### 服务器部署影响

- 不需要后端改动或重启
- 需要重新构建并部署前端静态资源（仅 LearnView.vue 改动）
- 不需要数据库迁移
- 不需要修改环境变量或 Nginx 配置

---

## 第 42 轮：性能与代码质量债务治理 — N+1 查询修复、CHECK 约束、迁移设计

### 修改时间

2026-07-02

### 修改目标

治理技术债务：修复作品列表的 N+1 查询问题（单个请求最多 81 条 SQL 降为 5 条），为数据库补充类型枚举 CHECK 约束，编写日期字段和用户主键的迁移设计文档。

### 修改内容

1. **作品 N+1 查询修复（高价值）**
   - 文件：`backend/app/services/project_service.py`
   - 新增 `with_project_eager_load(query)` 辅助函数：为作品查询添加 `joinedload(Project.author)`、`joinedload(Project.course)`、`selectinload(Project.images)`、`selectinload(Project.project_likes)`
   - 新增 `batch_load_liked_set(db, project_ids, user_id)` 辅助函数：批量查询用户已点赞的作品 ID 集合，替代逐条查询
   - 重写 `format_project()`：使用 relationship 替代 `db.query(User/Course/ProjectLike)` 逐条查询，移除所有 `hasattr`/`getattr` 防御式代码，新增可选 `liked_set` 参数
   - 修改 `list_approved_projects`、`get_user_projects`、`get_project`、`list_liked_projects`：全部使用 `with_project_eager_load()`

2. **路由层批量点赞查询**
   - 文件：`backend/app/api/v1/routes/project_routes.py`
   - 作品广场和"我的作品"列表端点使用 `batch_load_liked_set()` 预查点赞状态，传入 `format_project`

3. **教师端作品列表预加载**
   - 文件：`backend/app/services/teacher_service.py`
   - `list_all_projects` 使用 `with_project_eager_load()` 预加载，count 用独立查询避免 joinedload 影响
   - 文件：`backend/app/api/v1/routes/teacher_routes.py`
   - 教师端作品审核列表使用 `batch_load_liked_set()` 预查点赞

4. **schema_compat 补充 CHECK 约束**
   - 文件：`backend/app/db/schema_compat.py`
   - 新增 `_ensure_check_constraint()` 辅助函数（仅 MySQL，版本不兼容时静默跳过）
   - 为 `materials` 表添加 `ck_materials_type` CHECK 约束：`type IN ('video', 'pdf', 'link')`
   - 为 `questions` 表添加 `ck_questions_type` CHECK 约束：`type IN ('choice', 'fill', 'multi_choice')`

5. **日期字段迁移设计文档**
   - 新增：`docs/superpowers/specs/2026-07-02-date-field-migration-design.md`
   - 设计 `Material.date` 和 `Project.date` 从字符串迁移到 `created_at` DateTime 的分阶段方案

6. **用户主键迁移设计文档**
   - 新增：`docs/superpowers/specs/2026-07-02-user-primary-key-migration-design.md`
   - 设计新增 `User.numeric_id` 整型内部主键、逐表迁移外键的分阶段方案

### 性能改善对比

| 端点 | 修复前 SQL 数 | 修复后 SQL 数 | 改善幅度 |
|------|-------------|-------------|---------|
| `GET /projects`（12 条/页） | ~49 | ~5 | -90% |
| `GET /projects/mine`（12 条/页） | ~49 | ~5 | -90% |
| `GET /teacher/projects`（20 条/页） | ~81 | ~5 | -94% |
| `GET /projects/{id}`（单条） | ~5 | ~2 | -60% |
| 赞过的作品（无分页上限） | 无上限 | ~5 | 固定 |

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/services/project_service.py` | 修改（eager_load 辅助函数、format_project 重写、列表查询预加载） |
| `backend/app/services/teacher_service.py` | 修改（list_all_projects 预加载） |
| `backend/app/api/v1/routes/project_routes.py` | 修改（batch_load_liked_set） |
| `backend/app/api/v1/routes/teacher_routes.py` | 修改（batch_load_liked_set） |
| `backend/app/db/schema_compat.py` | 修改（CHECK 约束辅助函数 + materials/questions 枚举约束） |
| `docs/superpowers/specs/2026-07-02-date-field-migration-design.md` | 新增 |
| `docs/superpowers/specs/2026-07-02-user-primary-key-migration-design.md` | 新增 |

### 验证情况

- 后端 189 项测试全部通过
- `npm run build` 通过（含 vue-tsc type-check）
- CHECK 约束仅对 MySQL 生效，SQLite 测试库不受影响

### 服务器部署影响

- 需要拉取代码并重启后端服务（路由、服务、schema_compat 均有改动）
- 不需要重新构建前端（本轮无前端改动）
- schema_compat 会自动为 MySQL 补建 CHECK 约束，无需手动数据库迁移
- 不需要修改环境变量或 Nginx 配置

---

## 第 44 轮：游客行页面开放与答题流程简化

### 修改时间

2026-07-03

### 修改目标

让未登录游客可以浏览“行”页面和行动图文详情，同时保持作品列表、作品详情、点赞和上传仍需登录；简化学生答题流程，答对后直接进入下一题，答错后停留当前题查看解析。

### 修改内容

1. **“行”页面游客访问**
   - 文件：`frontend/src/router/index.ts`
   - `/act` 和 `/act/showcase/:id` 增加公开路由标记。
   - 保持教师端学生业务路径拦截早于公开路由放行，教师账号访问 `/act` 仍回到 `/teacher`。

2. **游客作品区降级**
   - 文件：`frontend/src/views/ActView.vue`
   - 页面只在已登录用户状态下调用作品列表接口。
   - 游客仍可加载公益课和读书会图文内容，但不会触发 `/api/projects`。
   - 游客在落地项目区域看到“登录后查看同学们的实践成果”和“去登录”按钮。

3. **学生答题节奏调整**
   - 文件：`frontend/src/views/PracticeQuizView.vue`
   - 提交答案后先记录答案和结果。
   - 答对非最后一题时直接进入下一题，不展示正确题解析。
   - 答对最后一题时直接进入完成总结；作业模式复用现有完成任务逻辑。
   - 答错时停留当前题，展示正确答案和解析，学生再点击“下一题”或“完成练习”。

4. **静态回归检查**
   - 新增：`frontend/tests/act-guest-access-static.test.mjs`
   - 新增：`frontend/tests/practice-quiz-flow-static.test.mjs`
   - 覆盖游客行页面公开路由、游客不触发作品接口、教师端拦截顺序、答对自动跳题、答错才展示解析等规则。

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `frontend/src/router/index.ts` | 修改（开放行页面公开路由） |
| `frontend/src/views/ActView.vue` | 修改（游客不请求作品接口，作品区显示登录提示） |
| `frontend/src/views/PracticeQuizView.vue` | 修改（答题正确自动进入下一题，错误保留解析） |
| `frontend/tests/act-guest-access-static.test.mjs` | 新增 |
| `frontend/tests/practice-quiz-flow-static.test.mjs` | 新增 |
| `docs/superpowers/project-map.md` | 修改（同步稳定事实和部署影响） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮记录） |

### 验证情况

- `node .\tests\act-guest-access-static.test.mjs` 通过
- `node .\tests\act-showcase-detail-static.test.mjs` 通过
- `node .\tests\teacher-workbench-only-static.test.mjs` 通过
- `node .\tests\public-learning-static.test.mjs` 通过
- `node .\tests\practice-quiz-flow-static.test.mjs` 通过
- `npm run build` 通过（含 `vue-tsc --build` 和 Vite 生产构建；仅保留既有大 chunk 警告）
- `python -m pytest backend\tests\test_public_learning.py` 通过（4 项）
- 独立代码审查发现过期登录态访问公开路由会被拦到登录页；已补静态断言并修复为清除过期登录态后对公开路由和首页按游客身份放行。

### 服务器部署影响

- 需要服务器拉取前端代码并重新构建、部署前端静态资源。
- 不需要后端重启。
- 不需要数据库迁移。
- 不需要修改环境变量或 Nginx 配置。

---

## 第 43 轮：深色背景品牌图标对比度增强

### 修改时间

2026-07-03

### 修改目标

修复统一网站图标在深色页脚和登录/注册页品牌渐变背景上对比度不足的问题，避免图标线条与背景色重叠导致识别不明显。

### 修改内容

1. **页脚品牌图标增加浅色底托**
   - 文件：`frontend/src/components/AppFooter.vue`
   - 在页脚品牌图标外增加 `footer-logo-mark` 承托层。
   - 使用宣纸白半透明背景、细边框和阴影，让图标在深色页脚中清晰可见。
   - 将品牌文字样式从通用 `.footer-logo span` 收窄为 `.footer-logo-text`，避免影响图标承托层。

2. **登录/注册页品牌图标增加高对比承托**
   - 文件：`frontend/src/views/LoginView.vue`
   - 文件：`frontend/src/views/RegisterView.vue`
   - 为左侧品牌区图标容器增加 `brand-logo-mark` 类。
   - 使用固定尺寸浅色底托、细边框和阴影，提升深色渐变背景上的识别度。

3. **静态回归检查**
   - 文件：`frontend/tests/site-icon-replacement-static.test.mjs`
   - 新增断言：深色背景中的统一网站图标必须保留高对比底托结构和浅色承托背景。

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `frontend/src/components/AppFooter.vue` | 修改（页脚品牌图标底托与文字样式收窄） |
| `frontend/src/views/LoginView.vue` | 修改（登录页品牌图标底托） |
| `frontend/src/views/RegisterView.vue` | 修改（注册页品牌图标底托） |
| `frontend/tests/site-icon-replacement-static.test.mjs` | 修改（新增图标对比度静态检查） |

### 验证情况

- `node ./tests/site-icon-replacement-static.test.mjs` 通过
- `npm run build` 通过（含 vue-tsc type-check）
- 已通过本地预览截图检查登录页和首页页脚，图标与深色背景不再重叠

### 服务器部署影响

- 不需要后端改动或重启
- 需要重新构建并部署前端静态资源
- 不需要数据库迁移
- 不需要修改环境变量或 Nginx 配置

---

## 第 45 轮：生产环境种子数据与虚拟数据清理

### 修改时间

2026-07-05

### 修改目标

删除生产环境中所有虚拟/测试数据的创建路径和残留代码，修复项目修改记录中的乱码条目，确保生产环境启动后不再执行任何种子数据逻辑。

### 修改内容

1. **`seed_data.py` 清空为安全占位函数**
   - 文件：`backend/seed_data.py`
   - `seed()` 函数体完全清空为 `pass`，不再创建任何数据库记录
   - 保留函数签名供下游测试（`test_seed_data_does_not_create_default_admin`）调用
   - 注释说明：生产环境初始化管理员请使用 `py scripts/create_admin.py` 而非种子数据

2. **`main.py` 移除 seed 互斥锁和启动调用链**
   - 文件：`backend/main.py`
   - 删除 `_seed_if_empty()` 函数（含 `fcntl` 文件锁、`_SEED_LOCK_FILE`、多 worker 互斥逻辑）
   - 删除 `lifespan` 中调用 `_seed_if_empty()` 的启动逻辑
   - 不再依赖 `sys.platform` / `fcntl` / `seed_data` 模块

3. **`database_setup.py` 移除 seed_data 调用**
   - 文件：`backend/database_setup.py`
   - 删除 `seed_data()` 函数和主流程中 `[3/3]` 阶段对它的调用
   - 部署说明标记为"跳过种子数据（seed_data.py 已清空）"

4. **`mock_demo_data.py` 禁用为纯注释文件**
   - 文件：`backend/mock_demo_data.py`
   - 将全部演示数据代码（USERS/COURSES/COURSE_MATERIALS 字典及 ensure_* 函数）替换为注释说明
   - 生产环境禁止执行此脚本

5. **`README.md` 更新管理员初始化说明**
   - 文件：`backend/README.md`
   - "系统不会再通过 seed_data.py 自动创建默认管理员" → "管理员初始化由运维通过独立脚本执行"

6. **修复修改记录乱码（第 30 轮）**
   - 文件：`backend/docs/项目修改记录.md`
   - 第 2018~2061 行：将 `UTF-8` 被误存为 `Latin-1` 导致的乱码内容替换为正确中文
   - 恢复标题"公共课程题库共享与负责记录"及全部中文正文

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/seed_data.py` | 修改（清空为安全占位函数） |
| `backend/main.py` | 修改（删除 seed 互斥锁及调用链） |
| `backend/database_setup.py` | 修改（移除 seed_data 调用） |
| `backend/mock_demo_data.py` | 修改（禁用为纯注释文件） |
| `backend/README.md` | 修改（更新管理员初始化说明） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮记录 + 恢复乱码条目） |

### 验证情况

- `py -m py_compile seed_data.py main.py database_setup.py mock_demo_data.py`：全部通过
- `py -m pytest tests/test_auth.py -q`：21 passed，`test_seed_data_does_not_create_default_admin` 仍通过（兼容空函数）

### 服务器部署影响

- **需要拉取代码并重启后端服务**（`main.py` 删除了 seed 调用链，后端启动日志不再有 seed 相关输出）
- **不需要重新构建前端**（本轮无前端改动）
- **不需要数据库迁移**（无表结构变更）
- **不需要修改环境变量或 Nginx 配置**
- **服务器上已有的业务数据不受影响**：所有改动只删除代码层创建路径，不触碰数据库现有记录
- **如果服务器上曾执行过旧版 seed_data.py 创建了数据**，不会自动删除；如有需要可单独处理
- **如果本地使用 `database_setup.py --reset` 重置数据库**，重置后将不再填充任何种子数据，需要用 `scripts/create_admin.py` 手动创建管理员

---

## 第 46 轮：作业评分系统（简化版）

### 修改时间

2026-07-10

### 修改目标

实现作业自动计分功能，学生答题后自动计算分数并持久化到 `task_completions` 表，教师导出成绩时直接读取持久化分数，避免每次导出时重新计算，提升性能并支持后续扩展手动改分。

### 修改内容

1. **数据库表扩展**
   - 文件：`backend/app/db/schema_compat.py`、`backend/app/models/entities.py`
   - `task_completions` 表新增 `score`（最终得分，可空）、`max_score`（满分，默认 100.0）字段
   - `announcements` 表新增 `max_score`（作业满分，默认 100.0）、`question_scores`（每题分值配置，JSON，可空）字段
   - 兼容层自动为既有表补齐新字段；新库由 ORM 模型创建

2. **答题自动计分逻辑**
   - 文件：`backend/app/services/quiz_service.py`
   - 新增 `_update_assignment_score()` 函数：学生每次答题后自动触发
   - 查询该作业所有题目的最新答题记录，计算正确率 × 满分 = 得分
   - 更新或创建 `TaskCompletion` 记录，将 `score` 和 `max_score` 持久化
   - `submit_answer()` 提交答题后，如果 `announcement_id` 不为空，自动调用计分函数

3. **成绩导出优化**
   - 文件：`backend/app/services/teacher_service.py`
   - `_latest_task_scores()` 优先从 `task_completions.score` 读取持久化分数
   - 如果记录无分数（兼容旧数据），则实时计算（保持向后兼容）
   - 导出时直接使用持久化分数，不再每次导出都重新遍历答题记录

4. **测试覆盖**
   - 文件：`backend/tests/test_assignment_scoring.py`（新增）
   - 5 项测试用例：
     - 答题后自动计分
     - 重新答题后分数更新
     - 导出使用持久化分数
     - 向后兼容旧数据（无持久化分数时实时计算）
     - 自定义满分（非 100 分）

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/db/schema_compat.py` | 修改（补齐评分字段） |
| `backend/app/models/entities.py` | 修改（导入 Float、新增评分字段） |
| `backend/app/services/quiz_service.py` | 修改（导入 Announcement/TaskCompletion、新增自动计分函数） |
| `backend/app/services/teacher_service.py` | 修改（成绩导出优先读取持久化分数） |
| `backend/tests/test_assignment_scoring.py` | 新增（5 项测试用例） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮记录） |

### 验证情况

- `pytest tests/test_assignment_scoring.py -v`：5 passed
- `pytest tests/test_lessons_progress.py tests/test_030405_management_systems.py -q`：22 passed（向后兼容验证）
- 所有测试通过，无回归问题

### 当前边界

- 第一版采用百分制自动计分（正确题数 / 总题数 × 满分）
- `announcements.question_scores` 字段已预留，可支持后续扩展每题分值配置
- 暂未提供教师手动改分界面，分数完全由系统自动计算；后续可扩展手动改分功能

### 服务器部署影响

- **需要拉取代码并重启后端服务**（兼容层会自动为既有表补齐 `score`、`max_score`、`question_scores` 字段）
- **不需要重新构建前端**（本轮无前端改动）
- **不需要执行 Alembic 迁移**（兼容层已自动处理；正式生产环境建议补 Alembic 迁移脚本）
- **不需要修改环境变量或 Nginx 配置**
- **对既有作业成绩无影响**：
  - 旧作业完成记录的 `score` 字段为 NULL，导出时会实时计算（兼容模式）
  - 学生重新答题后，系统会自动补写持久化分数
  - 不会修改或删除既有答题记录

- **生产环境正式初始化管理员请使用**：`py scripts/create_admin.py --id <管理员账号> --name <管理员姓名> --password <强密码>`

---

## 第 47 轮：Codex 代码审查与缺陷修复

### 修改时间

2026-07-10

### 修改目标

对 Codex 实现的功能增强（Redis 缓存、软删除、审计日志、课程进度等）进行全面代码审查，修复发现的 4 个严重缺陷。

### 审查范围

- 77 个文件变更
- 约 4698+ 行新增代码
- 228 个自动化测试

### 发现的问题

**严重缺陷（P0）**：
1. **Question 创建参数冲突** - `Question(**data, star_rating=...)` 导致参数重复传递，题目创建功能完全不可用
2. **delete_course 死代码** - return 语句后有无法执行的 db.commit() 和 return True
3. **软删除测试不兼容** - 删除逻辑改为软删除，但测试期望物理删除
4. **Redis API 弃用** - 使用已弃用的 setex() 方法

**中等问题（P1）**：
5. **Redis 服务未启动** - 本地环境无 Redis，所有测试都有连接失败日志（虽然有 fallback 机制）
6. **作业完成校验缺陷** - 学生未答完全部题目即可标记完成（业务逻辑 bug）

### 修复内容

1. **修复 Question 参数冲突**
   - 文件：`backend/app/services/question_service.py:120-127`
   - 修复前：`Question(**data, created_by=..., star_rating=...)` 直接展开字典
   - 修复后：先过滤冲突字段 `question_data = {k: v for k, v in data.items() if k not in ["created_by", "stem_hash", "star_rating"]}`
   - 验证：6 个题目创建测试从失败变为通过

2. **删除 delete_course 死代码**
   - 文件：`backend/app/services/question_service.py:292-307`
   - 删除第 306-307 行无法执行的代码
   - 调整 db.commit() 位置到 return 之前

3. **适配软删除测试**
   - 文件：`backend/tests/test_integration_bugfixes.py`
   - `test_delete_course_with_child_data_cleans_up_related_data` - 从验证物理删除改为验证软删除标记
   - `test_teacher_can_delete_owned_course_copy_without_affecting_public_source` - 同上
   - 断言从 `is None` 改为 `deleted_at is not None`

4. **修复 Redis API 弃用**
   - 文件：`backend/app/core/cache.py:59, 136`
   - 修复前：`redis_client.setex(key, ttl, value)`
   - 修复后：`redis_client.set(key, value, ex=ttl)`

### 验证结果

**测试统计**：
- 修复前：222 passed, 6 failed, 3 errors
- 修复后：226 passed, 2 failed, 3 errors
- 改善：4 个失败测试修复，测试通过率从 97.4% 提升到 98.3%

**剩余问题**：
- 2 failed：作业完成校验逻辑缺陷 + 重复题目拒绝断言不匹配（业务逻辑问题，非本次修复导致）
- 3 errors：部署环境检查测试（依赖外部配置，非代码缺陷）

### Codex 功能评估

**新增功能（整体评分：⭐⭐⭐⭐ 4/5）**：

✅ **已实现且质量良好**：
- Redis 缓存系统（课程列表、课程详情等）
- 软删除与回收站服务（支持数据恢复）
- 审计日志服务（完整操作历史）
- 课程进度追踪（course_progress 表）
- 通知系统扩展
- 题库优化字段（star_rating、stem_hash、created_by）
- Token 版本管理（强制登出支持）
- 用户软删除
- 数据库兼容层完善（schema_compat 自动补齐所有新字段）

⚠️ **建议改进**：
- 添加 Redis 开关配置（REDIS_ENABLED 环境变量）
- 修复作业完成校验逻辑
- 为新功能补充使用文档

### 修改文件清单

**后端核心修复**：
- `backend/app/services/question_service.py` - Question 创建参数修复 + 死代码删除
- `backend/app/core/cache.py` - Redis API 升级
- `backend/tests/test_integration_bugfixes.py` - 软删除测试适配

**文档**：
- `docs/superpowers/codex-code-review-2026-07-10.md` - 详细代码审查报告（包含所有问题、修复方案、评分）

### 服务器部署影响

**需要执行的操作**：
1. ✅ **拉取代码并重启后端** - 修复生效
2. ⚠️ **安装 Redis 依赖** - `pip install redis>=5.0.0`（如果未安装）
3. ⚠️ **启动 Redis 服务** - 或在 `.env` 中设置 `REDIS_ENABLED=false`
4. ✅ **数据库兼容** - 启动时自动补齐新字段（无需手动 SQL）

**不需要的操作**：
- ❌ 重新构建前端（本轮无前端改动）
- ❌ 手动数据库迁移（schema_compat 自动处理）
- ❌ 修改 Nginx 配置

### 遗留问题

1. **作业完成校验缺陷**（P1）- 学生未答完全部题目即可标记完成，需检查 `task_service.py` 的校验逻辑
2. **Redis 服务配置**（P1）- 建议增加开关，避免开发环境强依赖 Redis
3. **新功能手动测试**（P2）- 通知系统、课程进度、回收站等功能需人工验证

---

## 第 48 轮：多角度代码审查阶段 A 修复

### 修改时间

2026-07-10

### 修改目标

修复多角度审查发现的四类高风险问题：作业完成记录提前创建、密码变更后旧 JWT 仍可使用、教师通知越权、课程删除误伤全站共享题库。同步补齐前端改密后的替换 Token 保存。

### 根因与修复

1. **作业完成与事务**
   - 原因：评分辅助函数在部分答题后就创建完成记录，并在答题事务中存在多个提交点。
   - 修复：只有全部题目均已作答才创建 `TaskCompletion`；答题、评分和完成记录统一一次提交；并发唯一键冲突通过保存点恢复；手动完成接口始终先校验再返回历史记录。

2. **密码、JWT 与审计**
   - 原因：不同改密/重置入口没有统一递增 `token_version`，审批服务提前提交，部分路径缺少审计。
   - 修复：登录态改密、密保重置、教师/管理员审批和管理员直接重置全部轮换 Token；软删除用户不能登录或复用旧 Token；密码与审计在同一事务提交，审计失败时回滚；登录态改密返回新 Token。
   - 前端 `auth` store 统一替换内存和 `localStorage` Token，登录页、个人中心和独立改密页都通过 store 调用。

3. **教师通知权限**
   - 原因：发送接口只校验角色，没有校验目标学生是否属于教师班级。
   - 修复：教师单发跨班目标返回 403，批量发送过滤越权/已删除/偏好关闭目标；管理员可发送任意未删除学生；`action_url` 只允许站内路径。

4. **共享题库与课程删除**
   - 原因：课程软删除仍级联题目，教师编辑权依赖承接课程所有者，三条导入/新增路径没有完整写入创建人。
   - 修复：教师课程和公共课程删除前统一转挂有效题目；没有承接课程时拒绝删除；题目不进入课程软删除/恢复批次；编辑权依据 `Question.created_by`；教师和管理员新增、导入统一记录创建人。

### 验证结果

- 作业回归：15 passed, 1 warning。
- 认证、限流与审批回归：42 passed, 1 warning。
- 通知回归：4 passed, 1 warning。
- 课程、题库、公共课程删除与贡献日志回归：74 passed, 1 warning。
- 后端全量：`backend\.venv\Scripts\python.exe -m pytest backend/tests -q --basetemp=tmp/pytest-phase-a-full`，241 passed, 1 warning。
- 前端 Token 静态契约：`node frontend/tests/password-rotation-static.test.mjs`，通过。
- 前端类型检查：`npm run type-check`，通过。
- 前端生产构建：`npm run build`，通过；仅保留现有大 chunk 警告。

### 已关闭的上一轮遗留项

- “学生未答完全部题目即可标记完成”已关闭。
- “重复题目拒绝断言文案不一致”已关闭。
- Redis 开关、失败冷却和跨 worker 行为留在阶段 D 继续治理。

### 服务器部署影响

- 需要服务器拉取代码并重启后端服务。
- 需要重新构建并部署前端静态资源，确保改密后保存替换 Token。
- 部署前建议备份数据库；本阶段不新增表或字段，不需要数据库迁移。
- 不需要调整环境变量、Redis、Nginx 或其他服务器配置。

---

## 第 49 轮：多角度代码审查阶段 B 修复

### 修改时间

2026-07-11

### 修改目标

修复随机点名、作品入口、通知刷新、课时进度并发写入、非视频课时完成和课程分析规模问题，使前后端契约在真实访问次数、最终补报和学生分页上保持一致。

### 根因与修复

1. **随机点名与消息流程**
   - 随机点名不再读取班级摘要中的可选学生字段，改用 `getClassStudents(classId)`；切班时清理动画和旧结果，并用请求序号阻止慢请求覆盖新班级。
   - `/create` 继续保持登录边界，作品审核通知改为 `/create/project/{id}`。
   - 通知中心集中为 `loadMessages()`，恢复 15 秒轮询、页面重新可见刷新和跨组件刷新事件；通知偏好不进入轮询。页头最近通知只有标记已读成功后才跳转。

2. **课时进度原子写入与访问次数**
   - `LessonProgressIn` 新增 `visit_started: bool = False`。只有真实进入课时才增加 `view_count`，普通心跳和最终补报只累计时长。
   - SQLite 使用 `on_conflict_do_update`，MySQL 使用 `on_duplicate_key_update`；`LessonProgress` 与兼容用 `CourseProgress` 均通过原子 upsert 处理首次并发写入。
   - 时长、访问次数、最高百分比和完成状态使用数据库表达式更新，不再先读取 ORM 旧值后在 Python 累加。
   - 课程页路由组件以 `path` 为稳定 key；隐藏、`pagehide` 和卸载共用 `finalReportSent` 门闩，避免重复消费同一段时长。

3. **非视频课时显式完成**
   - `LessonReader` 向课程页报告是否含视频；已登录学生阅读无视频且未完成的课时时显示“完成本课”。
   - 完成动作带加载态，提交 100% 和尚未上报时长；成功、失败均显示中文反馈。视频课时继续按播放进度计算。

4. **课程分析数据库聚合与分页**
   - 分析接口增加 `page/page_size`，页码最小为 1，页大小限制为 1-100；`student_progress` 改为 `{items,total,page,page_size}`。
   - 有效学生子查询过滤已删除班级、已删除用户和非学生角色；学生指标与课时指标在数据库分组聚合，页面只加载当前页标量结果。
   - 教师课程详情增加 Element Plus 分页器；翻页和修改页大小只重新请求分析接口，请求期间禁用分页器。

### 验证结果

- 红灯确认：旧实现普通心跳误增访问次数，并发首次写入触发唯一键冲突；课程分析仍返回全量数组并实例化全部进度 ORM；前端缺少非视频完成与分页契约。
- 阶段 B 后端回归：`backend\.venv\Scripts\python.exe -m pytest backend/tests/test_lessons_progress.py backend/tests/test_progress_concurrency.py backend/tests/test_progress_analytics_scale.py -q --basetemp=tmp/pytest-phase-b`，`16 passed, 1 skipped, 1 warning`。
- 跳过项：未设置 `TEST_MYSQL_URL`，MySQL 并发用例未在本地执行；SQLite 并发用例通过。
- 前端全部静态测试通过。
- 前端生产构建：`npm run build --prefix frontend` 通过，仅保留既有大 chunk 警告。
- 未执行真实浏览器角色流程和 MySQL 实库压测，部署前仍需补验。

### 服务器部署影响

- 需要服务器拉取代码并重启后端服务，使进度 upsert 和分析分页接口生效。
- 需要重新构建并部署前端静态资源，使随机点名、通知刷新、非视频完成和教师分析分页生效。
- 本阶段不新增数据库表或字段，不需要数据库迁移。
- 不需要修改环境变量、Redis、Nginx 或其他服务器配置。
- 部署前建议在 MySQL 测试库运行并发进度用例，并以学生、教师账号完成关键浏览器流程。

## 阶段 C：文件鉴权与流式预览修复（Task 4 + Task 5）

### 修改时间

2026-07-12

### 修改目标

将作品封面、教师审核报告、管理员 PDF 预览等剩余私有文件消费者迁移到短时签名 URL 方案，杜绝普通登录 JWT 出现在 URL 中；并在本地 local 存储环境下验证签名 URL、文件流式传输、Range 请求和权限控制全链路。

### 修改内容

1. **新增 `AuthenticatedFileImage.vue` 通用组件**
   - 接受 `fileId` 与回退 URL，内部调用 `useAuthenticatedFileUrl` 获取签名 URL 并透传 `$attrs` 到真实 `<img>`；
   - 加载失败时携带实际失败 URL（非 objectURL）调用 `renew()`，同一 signedUrl 最多续签一次；
   - 无 displayUrl 时渲染 SVG 占位，不触发网络请求。

2. **新增 `useAuthenticatedFileUrl.ts` 组合式函数**
   - 调用 `POST /api/files/{id}/access-url` 获取 5 分钟短时签名 URL；
   - `AbortController` 管理：每次 fetch 前 abort 上次、URL 切换时 abort、组件卸载时 abort；
   - `renewedSet` (Set) 防止无限续签；`refresh()` try-catch 兜底走 fallback；
   - `enabled` 选项控制惰性启动，`onUrlChanged` watcher 在 URL 变更时重置全部状态。

3. **迁移 `ProjectDetailView.vue` 封面和图片**
   - 封面 `coverImgSrc` 优先 `cover_file_id`（`/api/files/{id}`）→ 回退 `image_url`；
   - 作品图片 `projectImgSrc` 优先 `file_id` → 回退 `image_url`；
   - `@error` 回调传 `failedSrc` 参数做精准续签；`onBeforeUnmount` 清理 blob URL。

4. **迁移 `TeacherReviews.vue` 报告预览**
   - `reportSource` computed 优先 `report_file_id` → 回退 `report_url`；
   - 使用 `useAuthenticatedFileUrl(reportSource, { enabled: drawerVisible })` 获取签名 URL；
   - `PdfPreviewDialog` 改为接受 `fileId` 并使用组合式函数签名；
   - 批量下载继续使用 `downloadProjectReportsZip` blob 传输。

5. **更新 `AdminPublicCourses.vue` PDF 预览**
   - `previewMaterial` 传递 `file_id` 给 `PdfPreviewDialog`；
   - Excel/ZIP/CSV 下载继续使用 blob 逻辑，未做改动。

6. **Task 5 本地 local 存储验证**
   - 使用临时 SQLite + local 存储启动脚本（`C:\tmp\tongshi-phase-c\`），不修改项目 `.env`；
   - 预置学生 2025001/abc123、教师 T001/abc123、课程、选课、私有 PDF、MP4、作品报告；
   - API 验收 24 PASS / 0 FAIL：签名 URL 获取、文件体下载（PDF 含魔数）、Range 206 + Content-Range、无认证拒绝（code=401）、URL 安全检查（无 auth_token）、Accept-Ranges: bytes。

### 验证结果

- 阶段 C 后端回归：`78 passed, 1 warning`（test_auth + test_material_file_acceleration + test_local_storage_boundaries + test_storage_s3）。
- 前端全部静态测试通过（34 个 .test.mjs，含 protected-file-consumers、file-access-url、material-preview、local-file-preview、public-learning）。
- 前端生产构建通过，仅保留既有大 chunk 警告。
- `git diff --check` 通过，仅 LF/CRLF 提示。
- Task 5 API 验收 26/26 PASS（使用真实 2.67MB 屏幕录制 MP4，ftyp 魔数验证通过）。
- 未验证项：快速切换测试中 file_id=3 对学生无权限（教师创建的文件），权限模型正确故跳过；公开文件 Nginx X-Accel-Redirect 和真实 Nginx Range 处理需部署后验收。

### 服务器部署影响

- 需要服务器拉取代码并重启后端服务，使文件签名 URL 端点和文件流式传输生效。
- 需要重新构建并部署前端静态资源，使 AuthenticatedFileImage 组件和 useAuthenticatedFileUrl 组合式函数生效。
- 本阶段不新增数据库表或字段，不需要数据库迁移。
- 不需要修改环境变量、Redis 或其他服务器配置。
- 部署后建议验收：真实 Nginx 文件体传输和 Range 请求处理；快速切换作品时旧签名响应不覆盖新作品。

---

## 第 50 轮：单机部署入口固化

### 修改时间

2026-07-14

### 修改目标

固化当前单机服务器的可重复发布流程，避免后端依赖未同步、前端静态文件未发布或服务重启后未做健康检查的半部署状态。

### 修改内容

- 新增 `deploy/redeploy-server.ps1`：支持 `DryRun` 预览和真实 SSH 部署。
- 部署前检查远端仓库、Python 虚拟环境、`.env`、`frontend/.env.production`、前端发布目录和根分区剩余空间；至少保留 2GB 可用空间。
- 远端执行 `git pull --ff-only origin main`、后端依赖安装、前端构建、`rsync --delete` 静态同步、后端重启和 `/health` 轮询。
- 重启失败或健康检查失败时输出后端最近 80 行 journal，并以非零状态结束；脚本不执行 `git clean -fd`，不读取或输出环境文件内容。
- 更新 `deploy/README.md`，说明完整部署入口与服务器运维计划的边界。

### 验证结果

- 临时静态契约测试通过，覆盖必需远端命令、磁盘阈值、失败日志和 DryRun 行为。
- Windows PowerShell `-File` 语法检查通过；DryRun 不连接服务器，即使 `IdentityFile` 不存在也能完成预览。
- 服务器端 systemd、journal、Swap、Docker、备份 timer 和真实部署流程本轮未执行，未验证远端服务状态、磁盘空间、数据库备份结果或 Nginx 行为。

### 服务器部署影响

- 本轮未连接服务器，不需要服务器拉代码、重启服务、重新构建前端、执行数据库迁移或调整环境变量。
- 后续执行 `redeploy-server.ps1` 时，会拉取 `main`、安装后端依赖、重新构建并同步前端静态资源、重启 `tongshi-backend.service`；不改变数据库结构，也不自动执行本机备份和系统运维配置。
- 单机资源治理、journal/Swap 限制、备份和健康检查仍需按 `docs/superpowers/plans/2026-07-14-single-server-ops-hardening.md` 在服务器上单独执行和验收。

---

## 第 51 轮：「学」页教程站形态改造

### 修改时间

2026-07-14

### 修改目标

将「学」入口从书架式学习馆改为游客友好的教程列表，并收紧课程资料目录的阶段/资料层级与行高。

### 修改内容

1. **LearnView.vue**
   - 去掉书脊、翻开书、页级统计、最新资料条
   - 改为公开教程卡片网格；接通公开课 keyword 搜索与空结果
   - 保留公开课与已加入课合并、进度「继续学习」
2. **CourseDetailView.vue**
   - 资料目录：阶段头单行可折叠 + 资料行缩进单行选中态
   - 去掉右侧 guide-facts（类型/大小/页数/时长）
   - 文案：返回教程列表、公开教程阅读、教程目录、学习资料
3. **router/index.ts**：`/learn` 页面标题改为「学 · 公开教程」
4. 更新静态回归测试与计划/项目地图文档

### 验证结果

- `npm run build` 通过
- `public-learning-static`、`copy-consistency-static`、`course-detail-layout-static` 通过

### 服务器部署影响

- 需要：服务器拉代码并重新构建、部署前端静态资源
- 不需要：后端重启、数据库迁移、改 Nginx、改环境变量

---

## 第 52 轮：学生端公开文件链路修复

### 修改时间

2026-07-14

- 问题：公开「学」页面 PDF 预览因 ASGI 空响应携带错误 `Content-Length` 导致浏览器无法展开；「悟」页面正文图片只校验封面引用，正文 `content_blocks[].data.file_id` 被当作无权限文件；生产 Nginx 对上传目录无遍历权限时还会返回 403。
- 修复：公开资料和课程资料的 `X-Accel-Redirect` 响应不再声明数据库文件长度或 `0` 长度；文件权限服务纳入激活展示内容块图片引用；前端正文图片加载失败显示中文占位，不绕过鉴权；生产 Nginx 固化 `/_protected_uploads/` internal alias。
- 服务器处理：备份 `/etc/nginx/conf.d/tongshi.conf`，执行 `nginx -t`、reload，重启 `tongshi-backend.service`；将 `/data/tongshi/uploads` 组改为 `nginx` 并保持 `750`，仅授予 Nginx worker 最小遍历/读取权限。
- 验证：后端相关测试 46 passed；前端 type-check、静态测试和 build 通过；公网 PDF 返回真实 `%PDF-`，Range 返回 206；「悟」正文 PNG/JPEG 返回真实文件字节；Nginx 和后端服务均为 active。
- 服务器部署影响：本轮已完成服务器代码、前端静态资源、Nginx reload 和后端重启；无需数据库迁移或环境变量修改。后续上传目录权限需保持 `nginx` 组可遍历。

---

## 第 53 轮：PDF.js Worker MIME 修复

### 修改时间

2026-07-14

### 问题与修复

- 浏览器控制台显示 `pdf.worker.min-*.mjs` 被 Nginx 以 `application/octet-stream` 返回，PDF.js Worker 被浏览器严格 MIME 校验拒绝，课程页因此回退到“浏览器无法直接显示 PDF”。
- `deploy/nginx.conf` 新增 `.mjs` 专用 location，映射为 `application/javascript` 并继续使用 `try_files` 校验静态文件存在。
- `backend/tests/test_deploy_files_static.py` 新增 `.mjs` MIME 回归断言。

### 服务器部署影响

- 仅需同步 Nginx 配置并执行 `nginx -t`、`systemctl reload nginx`；无需后端重启、数据库迁移或环境变量修改。
- 验收要求：`curl -I /assets/pdf.worker.min-*.mjs` 必须返回 `Content-Type: application/javascript`，再用浏览器重新打开课程 PDF。

---

## 第 54 轮：共享题库闭环（添加人 / 星级 / 编辑归属）

### 修改时间

2026-07-14

### 问题与修复

- 教师端题库已是全站共享，却仍用 `source_question_id/is_synced` 渲染“公共/私有”标签，语义误导。
- 列表接口补齐 `created_by`、`creator_name`、`star_rating`，`is_owner` 改为题目创建人判定。
- 教师端增加添加人、星级列，新增/编辑支持 1-5 星；移除误导标签。
- 编辑与 Excel 导入补齐 `stem_hash` 同题干拦截，并写入默认星级。

### 验证

- 后端：`tests/test_public_question_contribution.py`、`tests/test_question_import_skip.py`、`tests/test_question_duplicate_candidates.py` 共 18 passed。
- 前端：`tests/teacher-question-bank-static.test.mjs` 通过；`npm run build` 通过。

### 服务器部署影响

- 需要服务器拉代码、**重启后端**、**重新构建并部署前端**。
- 不需要数据库迁移、调整 Nginx 或环境变量。
---

## 第 61 轮：统一软删除 Task5 历史报表与中文审计

### 修改时间

2026-07-15

### 问题与修复

- 清理快照补齐作业摘要/作品摘要等中文字段，便于清理后回填成绩与档案。
- `history_snapshot_service` 新增 `history_attempt_totals` / `history_completion_rows` / `history_project_rows` / `history_task_catalog`。
- 教师学生列表、作业完成报告、成绩导出、成长档案合并活跃数据与历史快照；已清理资源不返回可跳转详情 URL。
- 审计日志增加 `action_name` / `resource_type_name` / `status_name`，导出表头中文化，内部 `action` 筛选兼容保留。

### 验证

- `test_soft_delete_snapshots.py` + `test_030405_management_systems.py` + `test_soft_delete_cleanup.py`：`30 passed`。
- 未执行后端全量与前端构建；未修改服务器配置。

### 服务器部署影响

- 本轮仅本地实现与验证，**不需要服务器修改**。
- 发布后需拉代码并**重启后端**后，教师端历史成绩/完成报告与管理端审计中文展示才会生效；无需前端重建、数据库迁移、Nginx 或环境变量调整。

---

## 第 60 轮：统一软删除 Task4 到期快照与自动清理

### 修改时间

2026-07-15

### 问题与修复

- 新增 `soft_delete_cleanup_service.py`：仅北京时间周日 03:00 后执行；按七类资源保留期筛选；逐资源独立事务。
- 清理前生成无外键历史快照，再按显式顺序物理清理关联事实；课程删除只清理同批子资源，避免 ORM cascade 误删单独软删记录。
- 文件仅在无业务引用时删除对象存储与 `StoredFile`；失败写入中文审计并可下周重试。
- 新增命令入口 `backend/scripts/run_soft_delete_cleanup.py`，供外部周调度调用，不改服务器配置。

### 验证

- `test_soft_delete_cleanup.py` + `test_soft_delete_snapshots.py`：`13 passed`。
- 联合 Task1-4 软删除相关回归另有执行。
- 未执行后端全量与前端构建；未修改服务器配置。

### 服务器部署影响

- 本轮仅本地实现与验证，**不需要服务器修改**。
- 发布后若启用自动清理：需在服务器用现有外部调度于周日 03:00 调用 `python backend/scripts/run_soft_delete_cleanup.py`；无需 Nginx/环境变量变更，无需前端构建。数据库表 `history_snapshots` 需已存在（Task1 兼容建表逻辑）。


---

## 第 59 轮：统一软删除 Task3 读路径与文件过滤

### 修改时间

2026-07-15

### 问题与修复

- 补齐软删除后正常业务读路径过滤：作业列表/详情/未读数、作品列表/详情/点赞列表、教师统计与学生/作业导出范围、成长档案。
- 继续沿用已有文件访问二次校验：签名 URL 解码后重查活跃用户与资料/课程/作品状态。
- 访问控制、任务、练习、公共学习、题库查询继续排除软删除资源；新增/强化读路径回归测试。

### 验证

- `test_soft_delete_read_filters.py` + `test_material_file_acceleration.py` + `test_quiz_submit_scope.py` + Task2 入口相关：`102 passed`。
- 未执行后端全量与前端构建；未修改服务器配置。

### 服务器部署影响

- 本轮仅本地实现与验证，**不需要服务器修改**。
- 待合并发布时：拉代码、重启后端；无数据库迁移、无 Nginx/环境变量变更、无前端构建要求。


---

## 第 58 轮：统一软删除 Task2 公共入口收口

### 修改时间

2026-07-15

### 问题与修复

- 统一软删除 Task 2 的核心入口此前已将班级、作业、资料、作品、题目、教师课程删除改为软删除；本轮补齐公共课程/公共资料/共享题目正式删除入口。
- `delete_public_course`：改为 `soft_delete(..., action="course.delete")`，不再转挂题目，不再清空教师副本 `source_course_id` / `source_material_id` / `source_stage_id`，也不再清理已软删课程上的题库根引用。
- `delete_public_material`：改为软删除，不再调用 `delete_synced_materials`，保留文件、预览与教师副本引用。
- `delete_public_question` / `delete_public_questions`：改为统一软删除，移除 `_purge_question_references` 对答题记录与作业 `question_ids` 的物理清理；活跃作业引用拦截仍由统一 `soft_delete` 负责。
- 管理端公共课程删除相关路由文案同步为“软删除”语义。
- 回归测试从“物理删除/解绑”断言改为“软删除 + 关联事实保留”。

### 验证

- `backend/tests/test_soft_delete_lifecycle.py` + `test_public_course_delete.py` + `test_030405_management_systems.py`：`43 passed`。
- 追加：`test_public_question_contribution.py` + `test_integration_bugfixes.py` + 上述套件：`108 passed`。
- `backend/tests/test_soft_delete_read_filters.py`：`16 passed`。
- 未执行后端全量、前端构建；未提交代码。

### 服务器部署影响

- 本轮仅本地 worktree 改动与验证，**不需要服务器修改**。
- 待 Task 2 整体提交并发布时，再统一：拉代码、重启后端；本轮无数据库迁移、无 Nginx/环境变量变更、无前端构建要求。

---

## 第 57 轮：代码审查问题集中修复与验证收口

### 修改时间

2026-07-14

### 修复内容

- 管理员可从任一活跃公共课程入口编辑共享题库中的活跃教师贡献题，不再错误要求题目原始课程与管理入口相同。
- 教师端与管理员端新增、编辑、导入统一使用规范化题干哈希；全站活跃题库中的同题干冲突统一拦截或跳过，并兼容旧数据哈希为空的情况。
- 管理端公共课程、资料、题目以及共享题库辅助查询统一排除软删除记录和所属软删除课程。
- 已发布展示文件继续允许匿名读取；未发布展示项的封面、图库和内容块图片仅允许创建者管理员或教师读取，其他用户保持业务 404。
- 展示内容块图片检查改为按必要字段分批迭代，避免一次性加载全表 JSON；同时清理不可达死代码。
- 管理端单条、批量、全部删除共享题目时，确认文案明确教师贡献题、作业引用、答题记录及不可恢复风险。
- Nginx 的 `.mjs` MIME 与缓存规则收窄到 `/assets/`，避免误匹配其他路径；上传目录静态测试统一为 `/data/tongshi/uploads`。
- 修正一条过时的自由练习回归测试：全站共享练习题必须挂在公共课程下，私有课程题仍要求学生加入所属课程。

### 验证

- 后端定向回归：`74 passed`。
- 后端全量测试：`319 passed, 1 skipped`。
- 前端全部静态测试：`42 passed`。
- `npm run build`：类型检查与生产构建通过，仅有既有大分块警告。
- 部署脚本使用占位地址执行 `-DryRun` 通过；`git diff --check` 通过；`graphify update .` 已更新代码图谱。

### 服务器部署影响

- 正式发布需要服务器拉取或同步代码、重启 `tongshi-backend.service`、重新构建并发布前端。
- 需要同步新的 Nginx 配置，先执行 `nginx -t`，通过后 reload。
- 不需要数据库迁移，不需要调整环境变量。
- 服务器当前存在未提交后端文件和未跟踪的 `frontend/.env.production`，本轮未自动部署；正式发布前必须先核对并处理服务器脏工作区，不能直接覆盖。

---

## 第 56 轮：PDF 资料阅读自动加载与入口收敛

### 修改时间

2026-07-14

### 问题与修复

- PDF 长资料依赖手动点击“加载更多页面”，阅读连续性较差。
- 正文下方重复展示“打开原资料”，与右侧资料操作入口重复。
- `MaterialInlineReader.vue` 新增 `IntersectionObserver` 底部哨兵，接近底部 400px 时自动追加 2 页；全部页面加载后自动停止，切换资料或卸载组件时释放观察器。
- 删除正文下方 `.pdf-open-row`，保留右侧“打开原资料”和错误状态下的备用打开入口。

### 验证

- `frontend/tests/public-learning-static.test.mjs`、`local-file-preview-static.test.mjs`、`material-preview-static.test.mjs` 通过。
- `npm run type-check --prefix frontend` 通过。
- `npm run build --prefix frontend` 通过，仅保留既有大 chunk 警告。

### 服务器部署影响

- 本轮仅本地修改和验证，不同步服务器、不重启后端、不重新构建部署线上前端、不修改数据库、Nginx 或环境变量。

---

## 第 55 轮：管理员共享题库一键/批量删除

### 修改时间

2026-07-14

### 问题与修复

- 管理端题库页列出全站共享题，但单题删除仍校验题目必须挂在当前公共课，导致跨课挂载题删不掉。
- 新增批量删除接口，并在管理端提供“删除选中 / 一键删除全部”。
- 删除时清理答题记录，并同步剔除作业 `question_ids` 中的引用。

### 验证

- 后端：`tests/test_public_question_contribution.py` 相关用例通过。
- 前端：`tests/admin-question-batch-delete-static.test.mjs` 通过；`npm run build` 通过。

### 服务器部署影响

- 需要服务器拉代码、**重启后端**、**重新构建并部署前端**。
- 不需要数据库迁移、调整 Nginx 或环境变量。

---

## 第 62 轮：主线冲突整合与既有工作恢复

### 修改时间

2026-07-16

### 修改目标

将主仓在远端 `main` 更新前保存的未提交工作安全整合到最新主线，处理 stash 应用产生的冲突，同时不回退统一软删除、到期清理和历史快照统计规则。

### 修改内容

- 在隔离工作树的临时分支中处理 13 个文本冲突，保留当前主线的软删除保留期限制、资源 ID 转换、读路径过滤与历史统计快照实现。
- 合入原工作区中已完成的自由练习提交范围收紧、软删除读取/文件访问过滤、共享题库字段、学习页教程站、资料阅读与公共文件链路等改动及其回归测试、计划和项目地图文档。
- 不合入管理员回收站前端 Task6：未修改 `frontend/src/api/admin.ts`、`frontend/src/views/admin/AdminRecycleBin.vue`，也未新增其静态测试。
- 原始 stash 继续保留为回退备份，待人工确认后再决定是否清理。

### 验证

- 后端定向回归：软删除策略 37 项、资源删除与读取过滤 53 项、作业/练习/题库范围 84 项均通过。
- 后端全量测试：399 passed, 1 skipped, 1 warning。隔离工作树验证通过临时加载主仓既有 .env，未读取、修改或提交其内容。
- 前端：
pm run type-check、
pm run build 通过；全部 42 个静态测试通过。构建仅保留既有大分块警告。
- git diff --check 通过；真实 MySQL 数据回填、浏览器实机交互与服务器环境不在本轮本地验证范围内。

### 服务器部署影响

- 正式发布需要服务器拉取或同步新的 `main` 代码、重启后端服务、重新构建并发布前端静态资源。
- 不需要数据库迁移、Nginx 配置调整或环境变量变更。
- 服务器现有脏工作区必须由维护者先核对、暂存或备份，不能直接覆盖。

---

## 第 63 轮：教师端资料入口并入管理课程

### 修改时间

2026-07-16

### 修改目标

消除教师端「管理课程」与「查看资料 / 资料管理」的功能重复，资料维护统一走课程详情。

### 修改内容

- `TeacherCourses.vue`：已添加课程操作区只保留「管理课程 / 编辑 / 删除」，去掉「查看资料」；公共课程列表同步去掉独立「查看资料」入口。
- `TeacherLayout.vue`：侧栏移除「资料管理」导航项。
- `TeacherDashboard.vue`：快捷操作「上传资料」改为「管理课程」，指向 `/teacher/courses`。
- `router/index.ts`：`/teacher/materials` 兼容跳转到 `/teacher/courses`，避免旧链接 404。
- 文档：`frontend-guidelines.md`、`project-map.md` 同步入口规则。
- 未删除 `TeacherMaterials.vue` 文件本体，仅取消产品入口；后续如确认无回归依赖可再清理。

### 验证

- 本地对照：课程列表、侧栏、路由、文档入口已收敛到「管理课程」。
- 本轮未跑全量前端构建；相关静态测试仍读取 `TeacherMaterials.vue` 组件实现，不依赖侧栏入口。

### 服务器部署影响

- 需要重新构建并发布前端静态资源后，服务器教师端才会去掉资料管理入口。
- 不需要后端重启、数据库迁移、Nginx 配置或环境变量变更。

---

## 第 63 轮补充：侧栏文案统一为管理课程

### 修改时间

2026-07-16

### 修改目标

左侧导航不再保留“资料管理/课程管理”双入口语义，统一为「管理课程」。

### 修改内容

- `TeacherLayout.vue`：侧栏项由「课程管理」改名为「管理课程」；进入课程详情时该导航项保持高亮。
- 路由页标题同步为「管理课程」。
- `frontend-guidelines.md`、`project-map.md` 同步导航命名。

### 服务器部署影响

- 需要重新构建并发布前端静态资源。
- 不需要后端重启、数据库迁移、Nginx 或环境变量变更。

---

## 第 64 轮：管理课程阶段资料扁平紧凑展示

### 修改时间

2026-07-16

### 修改目标

管理课程详情「阶段资料」原先使用大图文卡片并完整展开预览摘要，列表过长、扫描成本高。改为扁平紧凑行式展示，仍保留封面、摘要、类型、元信息与管理操作。

### 修改内容

- `MaterialRichCard.vue`：新增 `compact` 模式（小封面、单行截断摘要、操作横排），默认模式不变，不影响学生端/其他入口。
- `TeacherCourseDetail.vue`：阶段资料与未分类资料改用 `material-flat-list` 并启用 `compact`。
- `material-preview-static.test.mjs`：补充紧凑模式与扁平列表断言。
- `frontend-guidelines.md`：补充阶段资料扁平展示约定。

### 验证

- `node frontend/tests/material-preview-static.test.mjs`：通过。
- 本地打开 `/teacher/courses/:id` 资料页签，确认阶段资料为紧凑行、摘要截断、操作按钮仍可用。

### 服务器部署影响

- 需要重新构建并发布前端静态资源后，服务器教师端课程详情资料区才会变为扁平紧凑样式。
- 不需要后端重启、数据库迁移、Nginx 配置或环境变量变更。

---

## 第 65 轮：侧栏「管理课程」直达课程详情管理页

### 修改时间

2026-07-16

### 修改目标

左侧导航「管理课程」原先进入课程列表；用户期望它与列表操作列中的「管理课程」一致，直接打开课程详情管理页（资料 / 课时 / 学习分析）。

### 修改内容

- `TeacherLayout.vue`：侧栏「管理课程」解析为最近管理的课程详情路径 `/teacher/courses/:id`（sessionStorage 记忆）；若无记忆则取首门已添加课程；无课程时仍进入列表。
- `TeacherDashboard.vue`：快捷操作「管理课程」同步优先跳转最近课程详情。
- `frontend-guidelines.md`：补充侧栏与列表操作同页约定。

### 验证

- 本地：登录教师后点击侧栏「管理课程」，应进入课程详情管理页，与点击列表「管理课程」页面一致。
- 详情页「返回课程管理」仍可进入列表做添加/新建/切换课程。

### 服务器部署影响

- 需要重新构建并发布前端静态资源。
- 不需要后端重启、数据库迁移、Nginx 或环境变量变更。

---

## 第 66 轮：阶段资料摘要多行展示

### 修改时间

2026-07-16

### 修改目标

教师端管理课程详情「阶段资料」紧凑模式下摘要仅单行约 72 字，可读性不足。改为多行展示更完整摘要。

### 修改内容

- `MaterialRichCard.vue` compact：摘要截断上限 72→180 字；CSS 由单行省略改为最多 3 行；封面略放大；操作区允许换行。
- `frontend-guidelines.md`：同步「多行摘要约 3 行可见」约定。

### 验证

- 本地刷新 `/teacher/courses/:id` 资料页签，摘要应显示多行关键内容，不再只有一行省略。

### 服务器部署影响

- 需要重新构建并发布前端静态资源。
- 不需要后端重启、数据库迁移、Nginx 或环境变量变更。


---

## 第 67 轮：移除教师端独立「资料管理」页面

### 修改时间

2026-07-16

### 修改目标

删除已无产品入口的 `TeacherMaterials.vue`、`/teacher/materials` 兼容路由和前端死代码，将「管理课程 -> 课程详情 -> 资料」固化为教师维护课程资料的唯一入口。

### 修改内容

1. **删除独立页面与兼容路由**
   - 删除 `frontend/src/views/teacher/TeacherMaterials.vue`
   - 从 `frontend/src/router/index.ts` 删除 `path: 'materials'` 及其 redirect，不再保留别名或兼容跳转
   - 直接访问 `/teacher/materials` 命中全局 `/:pathMatch(.*)*`，展示现有 404

2. **删除前端死导出**
   - 从 `frontend/src/api/material.ts` 删除仅被旧页调用的 `getAllMaterials` 与随之无用的 `PaginatedResult` 导入
   - 保留 `getCourseContents`、`createMaterial`、`updateMaterial`、`deleteMaterial`、`rebuildMaterialPreview` 等课程详情仍使用的 API
   - **不删除**后端 `GET /api/materials` 及其他资料接口；Routes / Services / Models / 数据库均未改动

3. **测试迁移**
   - 新增 `frontend/tests/teacher-materials-retirement-static.test.mjs` 退役契约
   - 将 `material-preview`、`local-file-preview`、`protected-file-consumers`、`copy-consistency` 中仍适用的预览/安全/文案断言迁到 `TeacherCourseDetail.vue`
   - 删除 `mobile-admin-layout` 与文案测试中仅属于旧跨课程页的断言；跨课程搜索/筛选/分页能力随旧页退役，不迁移

4. **文档同步**
   - 更新 `docs/superpowers/project-map.md`、`docs/superpowers/frontend-guidelines.md`、`AGENTS.md`

5. **顺手收口 type-check**
   - `TeacherLayout.vue` 的 `resolveManageCoursePath`：在 `items` 非空后仍对 `target` 做空值兜底，消除严格 TypeScript 下 `target` 可能为 `undefined` 的报错（工作区既有未提交导航改动）

### 验证情况

- 残留扫描：`frontend/src` 与除退役测试外的 `frontend/tests` 均无 `TeacherMaterials` / `/teacher/materials` / `getAllMaterials` 命中
- 前端静态测试：43/43 通过（含退役契约与 teacher-workbench-only）
- `npm run type-check` 通过；`npm run build` 通过（含 type-check + vite build）
- 浏览器（前端 `127.0.0.1:5173` 可用）：
  - 未登录访问 `/teacher/materials`：title=`页面未找到`，文案「你访问的页面不存在或已被移除」，URL 不跳转
  - 登录页可打开；页面提示「教师账号请联系系统管理员分配」
  - 使用提供的账号 `111` 登录：后端返回业务错误「密码错误，请重试」，未能进入教师工作台
- 未验证（登录失败，无法进入教师会话）：
  - 教师课程详情资料完整 CRUD（上传/编辑/预览/重建/删除）
  - 学生/游客 `/learn` 资料直读
  - 管理员公共课程资料列表

### 服务器部署影响

- **需要**服务器拉取前端代码并重新构建、部署前端静态资源
- **不需要**重启后端服务
- **不需要**数据库迁移、清表或数据回填
- **不需要**修改环境变量、Nginx、Redis 或文件存储配置
- 部署后强制刷新一次，验证 `/teacher/materials` 为 404，且 `/teacher/courses/:courseId` 资料管理正常
